# 开发路线图

本路线图区分四种状态：格式可解析、资产可转换、数据可在 Godot 中运行、任务可完整游玩。前面三项不能替代最后一项。

## M0：工程与格式基线（已完成）

- [x] Patch/Remake 双子项目仓库结构；
- [x] Godot 4.7.1 与 .NET 10 工具链；
- [x] 合成 fixture 测试和意外资产收录守卫；
- [x] 1394 条 GFL 枚举、GBK 名称解码和伴随索引交叉校验；
- [x] 已知版本 SHA-256 校验；
- [x] LZO1X、RGB565、alpha 和黑色透明键；
- [x] IBLOCK、TLG1、SPR1、DBL、VWF、SLIST1 和 SLF/WAV 的严格解析；
- [x] 十二个正式 VWF/SLIST 精确解析到 EOF。

退出条件已经满足：工具可以拒绝未知版本，自动测试不依赖批量原版资产，无资产时 Godot 工程也能安全启动。

## M1：完整本地资源管线（已完成）

- [x] 34 张 IBLOCK PNG；
- [x] 45 张 TLG 4×4 图集 PNG；
- [x] 980 张 SPR 预览；
- [x] 980 份动画清单、2,775 个组 atlas 和 11,898 个逐帧 PNG；
- [x] 20 动作 × 9 方向语义，`serial = action × 9 + direction`；
- [x] 128 个 WAV；
- [x] `m000`—`m011` 十二关地形和 JSON，共 19,199 个实体；
- [x] 资源、动画和关卡索引清单；
- [x] Godot 通用动作加载器与十二关切换。

退出条件已经满足：已知版本的全部目标资源可由一条命令重复生成，转换计数与清单都有自动校验。

## M2：十二关数据恢复与运行时基线（当前）

- [x] 十二关地形、实体坐标、巡逻点和任务锚点只读显示；
- [x] 五名队员从真实关卡坐标生成；
- [x] 八方向跑步，缺失时回退行走；
- [x] 停止时使用对应方向的站立帧；
- [x] 通用加载任一已知非保留动作；
- [x] 十二关任务标题、目标依赖、计数、限时和失败条件数据化；
- [x] 通用任务状态机：事件匹配、去重、依赖、计数、失败与胜利；
- [x] `m011` 任务数据按简报要求六个目标，不沿用原版只检查最后目标的缺陷；
- [x] 验证 VWF L1—L5 主要语义与 `scene_index + 1000` 占用约定；
- [x] 生成 `M37NAV1` 导航中间网格，建立 L3 A* 与 L2 格线视线核心；
- [x] 还原普通敌人/军犬感知、近远识别区、11 类攻击范围与 L2 攻击遮挡；
- [x] 导出阵营、特殊感知、方向、姿态、生命值和默认攻击字段，并用十二关统计交叉验证；
- [x] 玩家和 656 个正式关卡敌方角色使用同一套只读源层 + 动态占位架构；
- [x] 接入 516 条非空巡逻路线，以及敌人的发现、追击、攻击距离判定和最后位置搜索；
- [x] 任务事件发送器强制使用当前关卡 `scene_bindings`，并校验 m011 六个爆破 scene 的锚点类型；
- [ ] 解释与玩法相关的剩余 DBL/SLIST 扩展字段及非角色实体精确足印；
- [x] 完成玩家/敌人动态占位和十二关静态导航/真实资源回归；
- [x] 按 `parameters[2] + 1` 基础 tick 还原逐组动作帧保持；
- [x] 按原程序路径在攻击最后一帧复核并结算命中，死亡动作播放一次并保持末帧；
- [ ] 校准站立循环、受伤反馈、特殊攻击对象、动作过渡和特殊锚点；
- [ ] 建立十二关关键地标和截图的自动回归基线。

当前可以浏览十二关、选择队员、自动绕障碍移动并进行实际战斗；伤害、有限弹药、装填、末帧命中、受伤反馈、死亡动作、基础警报和任务世界事件已经接通。十二关共用同一事件协议，`m000` 已具备营救—护送—撤离的胜负闭环；其余关卡的通用击毙/掉落/爆破/占点/撤离可运行，但候选角色、区域边界和演出仍需逐关校准。

## M3：战术玩法垂直切片

- [x] 在 L3 A* 之上完成动态占位、目标预留、阻挡重规划和基础编队避让；
- [x] 多格足印、源参考坐标、对角迁移与退化巡逻路线均有回归，并对高密度寻路请求设防回归上限；
- [x] 在 L2 上完成原版方向感知、军犬特殊感知、近远区、匍匐远区规避和最后位置搜索；
- [x] 完成攻击声源半径、受击响应和友军死亡警报传播；
- [ ] 完成声音遮挡、尸体发现、级联调查和更高层走廊会车协调；
- [x] 接入武器伤害、有限弹药、自动/主动装填和任务物件交互；
- [ ] 完成通用背包、地图弹药拾取、武器切换及原版弹匣/装填参数校准；
- [x] 把射击、近战、受伤和死亡动作接到玩法事件；
- [ ] 用原版投射物/爆炸/状态对象替换 type 8—11 的直接伤害默认路径；
- [x] 把救援、击毙、拾取、爆破、占点和出口事件送入任务状态机；
- [x] 第一关营救—护送—撤离片段具备成功和角色死亡失败流程；
- [ ] 固定输入回放、状态哈希和帧时间基线。

当前已满足“真实世界事件推进成功/失败”的功能部分；固定输入自动回放、状态哈希和包含新战斗链路的帧时间基线仍未完成，因此 M3 尚未整体退出。

## M4：第一关完整流程

- [x] 完成第一关全部目标所需的救援、护送、出口、AI、战斗和胜负接线；
- [ ] 校准角色能力、武器、敌人反应、难度和数值；
- [ ] 恢复并人工校准对白、镜头、演出节奏和教程提示；
- [ ] 菜单、字幕、存档和无障碍设置；
- [ ] 现代窗口/无边框全屏、桌面分辨率、按键重映射和音量分组；
- [x] 可由本地资产补全、带 PCK 回退和冒烟测试的 Windows 本地试玩构建流程；
- [ ] 安装正式导出模板后的发布签名、安装包和跨机器验收。

## M5：十二关完整内容

- [x] 以通用运行时连接十二关任务图、scene 绑定和关卡锚点；
- [ ] 逐关校准剧情角色、触发半径、AI 配合和演出顺序；
- [ ] 对 m005、m006、m009 等原数据/简报差异作可追踪决定；
- [ ] 处理过场视频、字幕和关卡间状态；
- [ ] 提供 `m011` 修正版行为，视需要再增加严格复现原缺陷的兼容选项；
- [ ] 十二关长时间回放、兼容性和性能验收。

## 性能与兼容性目标

- 1920×1080、60 FPS；
- P95 帧时间不超过 16.7 ms，P99 不超过 25 ms；
- 正常游玩期间不出现超过 50 ms 的同步资源加载尖峰；
- 十分钟自动回放无状态漂移、无持续内存增长；
- 默认 Godot Compatibility renderer，可在 Windows 10/11 和常见旧集成显卡运行。

这些是最终验收目标。本轮战斗和任务接线后已重跑 m000 的 44 项窗口探针：360 帧维持 60 FPS，并通过双人营救—全员撤离—重置的真实 scene 闭环；本地试玩包的 headless 冒烟仍只证明可启动。后续仍需以固定输入覆盖长时间交战和掉落，建立状态哈希、同步加载尖峰和十分钟稳定性基线。
