# Windows 本地试玩包

本工程从仓库稳定 `Mod/` 重建本地转换资源，不把转换结果重复提交到普通
Git 历史。试玩包只在开发者电脑的
`Remake/LocalBuild/1937Remake/` 中生成，该目录已由根 `.gitignore` 排除。

当前开发机的默认启动入口为：

```text
F:\bigsinger\1937tezhongbing\Remake\LocalBuild\1937Remake\Play-1937-Remake.cmd
```

它是本机生成物；源码、任务或资源转换结果发生变化后应重新构建，不要把旧 PCK 当作最新试玩版本。试玩前请打开同目录 `build-info.json` 核对 `git_commit` 与当前源码提交；本地构建被 Git 忽略，不会因拉取源码自动刷新。

## 一键生成

在 `Remake` 目录运行：

```powershell
.\tools\Build-Playable.cmd
```

脚本会检查 `LocalAssets/manifest.json`。如果资源缺失或来源不是
`repository-mod-12-level-20260729`，会先自动执行 `Import-ModAssets.ps1`；
无需再手工指定原绿色版目录。

如果 Godot 4.7.1 不在 `PATH`，显式传入标准版控制台程序：

```powershell
.\tools\Build-Playable.cmd `
  -GodotExecutable "C:\Tools\Godot_v4.7.1-stable_win64_console.exe"
```

生成结果：

```text
LocalBuild/1937Remake/
├── Play-1937-Remake.cmd       双击启动入口
├── README.txt
├── build-info.json
├── smoke-test.log
├── executable-smoke-test.log
├── game/
│   ├── 1937Remake.exe
│   └── 1937Remake.pck
└── LocalAssets/               转换资源目录或本机目录联接
```

默认的 `Junction` 模式不会再次复制数百 MiB 转换资源，适合当前电脑快速反复构建。
生成可移动目录时使用复制模式：

```powershell
.\tools\Build-Playable.cmd -AssetMode Copy
```

复制模式只放入运行所需的 `converted/`，不会复制 `raw/`、转换测试目录或原始 GFL
提取结果。无论采用哪一种模式，生成目录都不会进入 Git。

本轮涉及输入、UI、特殊对象、十二关导演和存档 schema 的修改，必须重新运行构建脚本后再验收。构建脚本会先用 PCK 加载路径验证包，再直接启动复制/导出的 `game/1937Remake.exe`；成功至少应满足两个 smoke log 都无 Godot/GDScript 错误、十槽保存页可打开、`M` 地图红点持续刷新、`A`/`W` 为五列方格、设置页显示总静音、四条音量滑杆和按键列表。

## 运行与切关

双击 `Play-1937-Remake.cmd` 启动后会出现原生十二关选择页；当前关、已完成关
和顺序战役前沿会分别标记，但全部正式关都可直接试玩。游戏中也可由 `Esc`
菜单再次打开“选择关卡”。不要只移动 `game/1937Remake.exe`，因为当前运行时按
`game/` 的上一级寻找 `LocalAssets/converted/`；启动器也会固定进程工作目录，保证 PCK
运行方式下该相对路径稳定。

命令行可直接选择关卡：

```powershell
.\LocalBuild\1937Remake\Play-1937-Remake.cmd -- --level=m007
```

可用关卡 ID 为 `m000`—`m011`。`PageUp` / `PageDown` 只在调试构建中切关；`R` 已按原版改为跑步/行走切换，不再重置关卡。
自动探针或不希望显示简报时可追加 `--skip-briefing`。

稳定 MOD 的现代启动中心通过 SDK 路由直接开放全部十二关，这条“自由选关”
路径不等于原版内置战役解锁。Remake 保留命令行自由选关供开发、对照和试玩；
仅当任务状态机真正发出胜利时，才把本关写入完成列表并把顺序战役推进到下一
关。自由进入后退出、失败或读档都不会伪造通关记录。

## 当前操作

| 操作 | 输入 |
|---|---|
| 选择单个队员 | 左键队员 |
| 追加/取消选择 | `Shift` + 左键队员；右键只用于拖框多选，不提交世界命令 |
| 编队移动 | 左键地面；不会绘制人物到目标点的黄色命令线 |
| 地面拾取物 | 左键拾取物；最近的所选队员会先自动寻路，在横向 32、纵向 16 的导航格两轴各相差不超过一格时收入自己的原版容器 |
| 攻击 | 先选队员，再左键敌人或汽油桶；默认按住左 `Ctrl` 或 `↑` 再左键可强制目标/强制攻击，两条按住通道均可重映射 |
| 游戏菜单 | 松开 `Esc`；有地图、背包、指南、简报或媒体时优先关闭当前层 |
| 操作指南 / 任务简报 | `F1` / `F7` |
| 选择队员 | `F2`—`F6`：本关第 1—5 名可玩队员（角色随关卡阵容变化） |
| 跑/走、站立/匍匐 | `R` / `C`，应用到当前选中队员 |
| 武器栏 / 物品栏 | `W` / `A`；右侧 276×421 五列方格，二者互斥，再按相同键关闭 |
| 右下角地图 | `M`；不暂停战斗，红色敌军点等实时刷新，单击地图可卷屏，再按 `M` 或松开 `Esc` 关闭 |
| 视野观察 | `S`；复刻增强：显示敌军视野和攻击范围 |
| 阵亡目标处理 | `B` 后左击阵亡敌人；复刻对原版“掩埋模式”命令路径的可玩解释 |
| 快速保存 / 按时间读取最新有效槽 | `Ctrl+F5` / `Ctrl+F9`；菜单读取始终打开多槽选择器。原版 F5 用于选择本关第 4 名可玩队员，原版没有正式快存读档键 |
| 营救 | 由剧情指定角色靠近后自动触发；m001 必须由换上军服的古明接应司机，m002/m004 获救角色会加入可操作队伍 |
| 任务掉落和爆破点 | 靠近后按 `E`；`E` 也保留为早期试玩包的近距离拾取兼容入口 |
| m008 手动引爆 | `F`；仅 `repaired` 规则要求，默认 `stable_mod` 在四处布置后直接由老赵、大牛撤离 |
| 主动装填 | 选中队员后按 `Q` |
| 直接选择已持有项目 | `1`—`0`：匕首、弹弓、大刀、飞刀、手枪、步枪、机枪、地雷、手榴弹、炸药包 |
| 轮换已持有武器 | `Tab`；`Shift + Tab` 反向 |
| 关闭简报/下一句，或跳过媒体 | `Enter` / `Space` 继续；松开 `Esc` 跳过（模态媒体期间任务计时和战斗暂停） |
| 平移镜头 | 鼠标移到客户区左/上坐标 0—1 或右/下最后一列/行，按原版 8 步加减速卷屏；左右/下方向键或中键拖动是复刻扩展，原版 `↑` 保留为强制目标按住键 |
| 缩放 | 鼠标滚轮 |

`S`/`B` 的原版帮助图标签分别是“视线观察模式”和“掩埋模式”，但地址级运行路径进一步表明：S 会把下一次存活友军左击变为单选/观察目标，B 会进入尸体或失能友军目标命令。当前复刻的范围显示和阵亡敌人清理是明确的增强/可玩解释，并未声称逐语义复现。完整原版键表、F8/F9 调试残留和鼠标证据见[原版行为取证摘要](ORIGINAL_BEHAVIOR_FORENSICS.md)。所有表中可重映射的键都可在“设置 → 按键设置”修改；发生冲突时两个动作交换绑定，也可以恢复默认值。

界面右侧显示任务进度，左上显示所选队员的武器、原版直接数量/耐久状态、生命和地雷。`A`/`W` 弹出的背包按原版运行代码采用 276×421、右侧对齐的五列方格，数量和当前装备状态直接显示在格内。出口每 0.2 秒自动检查 128 像素范围；默认要求全部存活队员和本关护送角色，存在 `exit_party` 规则时按剧情指定成员判定，例如 m001 只要求古明与铁蛋爹、m002 要求老赵与获救的强子、m011 稳定规则要求老赵与强子。m010 不使用 `E`：四个 128 像素制高点必须分别有老赵、强子、大牛、古明之一，真实地图距离使四人必须同时分处四地；按 `M` 打开地图时，四个占点位置会显示为任务标记。

原版 M 地图只是右下角的逐关静态烘焙图，不含动态红点。复刻保持右下角和底栏上方的位置关系，同时增加实时我方/敌方/任务点、镜头框和点击卷屏，属于有意的现代可玩性增强；打开地图不会暂停单位移动和 AI。

稳定 MOD 的原生爆破目标不再允许靠近后按 `E` 直接完成。m001/m002/m004 必须用手榴弹、油桶或定时炸药等真实世界爆炸摧毁目标；m003/m008 必须把物品 45 部署在每个标记严格 128 像素内，物品在部署命中帧消费一次。m009/m011 尚无专用求值器的兼容目标仍按界面提示执行数据化交互规则。重复、无效交互和任务层观察都不会重复扣除物品。m008 默认稳定规则在四处布置后直接撤离；只有显式选择 `repaired` 时才需再按 `F` 手动引爆，并启用提前引爆失败。

## 游戏菜单、设置与存档

正常游戏时松开 `Esc` 打开暂停菜单。菜单暂停任务计时、AI 和战斗，可继续游戏、自由选择十二个正式关卡、打开多槽存档读取器、保存、重新开始本关或退出；菜单内松开鼠标右键也可返回上一级。任务失败后，游戏画面会灰化并自动打开标题为“任务失败”的菜单，正文模板为 `任务失败：%s\n可重新开始本关，或从多槽存档选择器读取进度。`（`%s` 为失败原因）；继续和保存不可用，重玩本关和从任一有效槽读取可用，且失败状态不能覆盖已有有效存档。`R` 在游戏世界中始终是跑/走切换，不承担重开功能。

菜单中的设置会持久保存；显示、声音和界面项立即应用，难度在重开/切关/
读档时应用：

- 原版复刻、轻松、标准、困难四种难度；默认原版复刻不启用补写的 AI/数值调校，存档携带自身难度；
- 可明确选择默认 1280×720 窗口、跟随 Windows 当前桌面分辨率的桌面全屏，或适合多任务切换的无边框最大化；
- 主音量、音乐、音效和语音四条滑杆，分别控制 `Master`、`Music`、`Sfx`、`Voice` 音频总线；
- 可捕获 Ctrl/Alt/Shift 组合键的按键重映射，以及一键恢复默认键位；
- 语音字幕；
- 切换关卡时是否显示任务简报；
- 鼠标移动到窗口边缘时是否卷屏；
- “减少自动镜头运动”会把剧情聚焦从平滑动画改成立即定位；
- “大号原版光标”以最近邻方式把原 GFL 光标放大到 2 倍，保持像素锐利，且不会捕获、吸附或移动系统光标。

保存/读取菜单提供 `slot_1`—`slot_10` 十个手动槽；覆盖已占用槽需再次点击确认。读取页还会列出存在的 `quick` 和 `autosave`，`Ctrl+F5` 写入 `quick`，`Ctrl+F9` 读取时间最新的有效存档，任务胜利时静默更新 `autosave`。任务失败状态不能覆盖有效存档。存档覆盖当前局内可变状态：

- 当前关卡、任务耗时、目标完成/计数/去重值、失败状态和持久任务事实；
- 队员、敌人和护送角色的位置、阵营、生命/死亡、匍匐/选择状态，玩家背包、武器、弹匣与备弹；
- 敌人巡逻/搜索状态，护送角色的获救与跟随关系；
- 已激活任务 scene、关卡公共物品、尚未拾取的地图物品、汽油桶剩余生命或已销毁状态、尚未取得的任务掉落；
- 已部署地雷、type 8/10 世界对象及 type 11 AI 控制状态、尚未结算的在途投射物；
- 已掩埋敌人的 scene 索引；读取后这些尸体保持隐藏且不重新参与处理；
- 十二关导演已触发节拍、教程门控/持久事件/计时，以及 AI 姿态、增援预算和命令序号；
- 镜头位置/缩放，以及已完成关卡和最高解锁关卡的战役进度。

静态地形、导航网格、纹理、节点引用和信号连接不会写入存档；读取时先从本地转换资源重建对应关卡，再恢复上述可变状态。未完成的鼠标移动/攻击命令、当前动画的精确帧和打开中的菜单/媒体层也不序列化，角色会从安全状态继续；已经离开发射者的世界投射物会恢复。

设置和存档均为带 `schema_version` 的 JSON。写入过程先在同目录生成临时文件、重新读取并校验，再替换主文件；上一个有效版本保留为 `.bak`。主文件缺失或损坏时会读取备份；下一次写入会先把损坏主文件隔离为 `.corrupt`，不会反过来覆盖已知有效备份。

存档当前 schema 为 1，明确支持内部原型 schema 0：读取时只在内存迁移，下次
主动保存才写为当前格式。版本高于 1 或低于 0 的可解析存档采用“拒绝且原样
保留全部代际”策略；旧程序不会把未来版本主文件/备份误判为损坏、改名为
`.corrupt` 或覆盖。十二关战役状态测试会逐次调用产品胜利入口，在每关之后
写入并重读真实 JSON，确认完成列表去重、解锁前沿不倒退且第十二关不回绕。

## 本轮试玩范围与已知边界

当前源码已经包含实际伤害、玩家背包与武器切换、原版 mode 0/1/2 直接数量、攻击动作末帧命中、飞镖/弹弓/手榴弹世界投射物、type 8/10/11 生命周期、受伤/死亡、攻击/友军死亡警报、真实关卡拾取、地雷、可爆汽油桶和任务角色掉落，以及救援—击毙—拾取—爆破/占点—撤离—胜负事件链。普通攻击还按原 `sub_456DF0` 恢复了 runtime type 条件伤害、机枪直接目标只命中一次和低伤害免疫。产品壳提供暂停/失败菜单、右下角地图、五列背包、边缘卷屏、十槽存档、按键重映射、分通道音量和失败灰化/重玩闭环。导入本地资产时，切关会显示原简报图；简报可用原版相同的窗口内左键在松开时关闭，也可用继续/系统菜单键关闭，模态层会吞掉该点击而不穿透到地图。独立音乐/环境声播放器和影片音轨进入 `Music`，模态对白及不会互相截断的并发角色语音进入 `Voice`；8 路预热 `Sfx` 池播放已分类的原攻击、命中、非语音警报和 UI WAV，忙满时扩展而不截断旧声音，缺失时安全降级。

十二关还接入了第一版数据驱动导演：43 个节奏节点、45 行提示对白、教程门控、镜头请求、AI 协作和逐关难度。任务/scene 引用可追溯到恢复数据；对白措辞、镜头参数、教程、AI 和难度均标为 `remake_editorial`。这意味着它们已可执行，不意味着原版逐字对白或原始平衡已经恢复。

当前源码已通过真实 1920×1080 窗口的十二关双轮 600 秒参考性能门禁：
33,125 个渲染帧、2,466 个仅目标视口输入事件，整体 P95 18.167 ms、
P99 20.936 ms、最大 47.976 ms、零个 >50 ms 帧；最密集的 m004 为
P95 18.941 ms、P99 22.824 ms。第二轮静态内存仅增长 1,063,804 字节，
24 次关卡载入均低于 3.4 秒。
测试不捕获、裁剪、锁定或移动系统鼠标。精简基线见
`validation/baselines/remake/campaign-performance-1920x1080-v1.json`；
这些数字只代表当前 Windows 10/GTX 1050 Ti 参考机，不能替代用户机器实测。

2026-07-31 在完整 RowLookup、稳定巡逻证据路径、紧凑 recovered A* 和
多格足印批量预计算合并后，全量 `Verify.ps1` 的 48 秒窗口复验为
2,160 帧、P95 17.703 ms、P99 20.323 ms、最大 34.686 ms，十二关
继续保持零个 >50 ms 帧。首个移动应答音频已改为关卡构造期静默预热，
实际点击不再承担 WAV 冷解码；这不会提前播放声音或改变应答变体顺序。

以下内容仍属于开发中，不应据此判断最终品质：

- `sub_458700` 已确认非致命伤只扣生命，不中断攻击、装填、移动命令或写入
  独立反馈计时；当前实现遵循该无硬直路径，致命伤才进入死亡动作；
- 敌人暂为无限弹药；玩家攻击恢复时间仍是重制默认值，原版玩家武器已改为直接数量；
- type 1/2/3/6/7/9 的逐 tick 步长、Bresenham/抛物线、目标格门、机枪三路散布、actor/GFL、L3→L2 碰撞、直接伤害、actor 60 命中火花、终点 actor 61 爆炸、SPR 发射锚点和中途存读档均已恢复。type 8/10/11 的部署—触发/计时—actor 62 精确伤害/几何/警报/五轮效果粒子—type 11 注意力保持—释放—存读档生命周期已实现；其他系统尚未恢复的全局 `rand()` 调用会影响特定时刻的粒子变体，但不会改变其 90/150 tick 寿命；
- m011 不编辑性发放项目 99；物品 99 已按原版恢复为古明持有军服 54、目标命令 101 与伪装/还原生命周期共同产生的运行时状态；
- 真实拾取实体身份、物品映射、容器/mode、每次拾取数量 1、精灵点击框和 32×16 邻格完成规则已恢复；地雷/油桶数值仍是重制默认；
- m004 已确认 scene 2637 携带物品 101；m006/m008/m009/m011 可在菜单选择默认的稳定 MOD 实际判定或按简报补全的修复增强规则，存档会保留选择；m006 的物品 101 拾取者、m008 原始撤离、m010 四区实时判定和 m011 最后出口均已由原控制流定案。其余逐关演出仍需持续校准；
- 原 WAV 与简报已经接线；十二关导演第一版已覆盖对白、镜头提示、教程、AI 配合和难度，但内容仍需原版录像证据升级与逐关完整通关调校。视频需先转码，尚未编排为十二关完整过场；
- S/B 的真实按键—目标点击—状态转换与简报左键关闭已经形成稳定 MOD/Remake 差分门禁；S/B 的原光标逐像素反馈、HUD/背包/小地图逐层像素、跨多显示器与不同 DPI 校准、Windows 11/更多硬件跨机器验证，以及稳定 MOD/Remake 真人完整通关差分仍需完成；窗口、桌面全屏和无边框最大化入口及其持久化已完成，多层 SPR/RowLookup 世界遮挡已经进入十二关视觉门禁。

自动验证已加入 type 8/10/11 专项生命周期套件、十二关导演套件、m004 高密度寻路和 48 秒真实窗口性能冒烟，并继续覆盖格式、核心逻辑、战斗/任务、投射物/背包、世界交互、媒体、产品壳、存档/设置、确定性回放和真实关卡/媒体。各套件会在日志中输出当前检查数，文档不复制容易随功能和 fixture 变化而过期的固定总数。完整发布候选性能可运行：

```powershell
.\tools\Run-CampaignPerformance.ps1 `
  -GodotExecutable D:\Godot\Godot_v4.7.1-stable_win64_console.exe `
  -DurationSeconds 600 -Passes 2
```

反馈问题时请同时提供 `build-info.json`、关卡 ID、复现步骤和截图；如果是启动失败，再附上 `smoke-test.log` 与 `executable-smoke-test.log`。

## 导出模板与回退方式

`game/export_presets.cfg` 提供 `Windows Desktop` 预设。构建脚本会检查
`%APPDATA%\Godot\export_templates\4.7.1.stable\windows_release_x86_64.exe`：

- 已安装匹配模板：生成标准 Godot release 导出；
- 未安装模板：导出相同内容的 PCK，并复制当前 Godot 4.7.1 标准版作为本地运行器。

后一种方式无需为了 Windows 本机试玩下载约 1.28 GB 的全平台模板包，生成目录仍可直接
运行；它只用于本地且不会提交。两种方式最后都会从生成目录启动 8 个 headless 帧，日志中
只要出现 Godot 或 GDScript 错误，构建即失败。

`build-info.json` 会记录 Git 提交、Godot 版本、构建方式和资产模式，便于确认反馈对应的源码版本。构建脚本只允许输出到 `Remake/LocalBuild/` 的子目录，并在替换旧输出前拒绝删除重解析点，避免误删目录联接目标。
