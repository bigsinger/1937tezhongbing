# 稳定 MOD 完全复刻开发实施方案

## 1. 最终目标与完成定义

`Remake` 的目标不是“能读取原资源的相似游戏”，而是在现代运行时中完整
复现稳定 `Mod/` 的十二关产品：

- 同一地图、五层地形语义、对象、角色、出生点、巡逻路线和遮挡关系；
- 同一默认快捷键、鼠标命令、选择规则、姿态、上下文光标和操作节奏；
- 同一初始背包、世界物品、武器、弹药、攻击命中帧、伤害和特殊动作；
- 同一视线、听觉、警报、搜索、增援、攻击和返回巡逻行为；
- 同一任务目标、依赖、对白、镜头、教程、过场、成功和失败闭环；
- 同一菜单、HUD、简报、小地图、背包、声音触发和关卡推进；
- 存档后读取能够从任务任意合法阶段继续，结果不漂移；
- 现代分辨率、窗口/全屏、按键重映射、分通道音量和稳定帧时间可作为
  实现升级，但不能改变原版玩法语义。

只有十二关的全部可观察规则都通过分解验证，主要失败路径和中途存读档也
通过，且
`game/data/mod_parity_contract.json` 中不再存在 `partial` 或 `missing`，
项目才可以标记为完全复刻。格式可解析、场景可显示、自动把事件直接送进
任务状态机，都不能单独算通关验收。十二关“开始游戏 → 真人实际操作 →
胜利”保留为发布前人工验收，不再要求开发工具自动玩通原版关卡。

## 2. 唯一内容基线

从 2026-07-29 起，仓库内稳定 `Mod/` 是复刻的唯一产品内容基线：

```text
repository-mod-12-level-20260729
├── 1937m000.vwf ... 1937m011.vwf
├── 1937Resources.GFL + InterMedia.GFL（1,394 项）
├── 1937Database.dbl
├── 1937Sound.slf
└── M1937.exe
```

`KnownStableModVersion` 对以上所有消费文件逐一做 SHA-256 校验；存档、
`rungame.ini` 和兼容 DLL 是运行状态或平台层，不参与内容身份。实验性
第 13—15 关、Sidecar 和追加简报不属于基线，也不能再次进入导入结果。

`tools/Import-ModAssets.cmd` 是默认导入入口。`Build-Playable` 若发现
`LocalAssets` 缺失或来源不是上述 profile，会自动从 `Mod/` 重建。原绿色版
目录仍可用于二进制取证，但不再作为试玩包的隐式依赖。

## 3. 证据优先级

每项行为按以下顺序定案：

1. 稳定 MOD 中真实运行的 `M1937.exe` 行为和窗口内录像/状态轨迹；
2. VWF、DBL、GFL、SLIST、SPR、SLF 等资源中的原始字段；
3. `SDK/` 中已做签名和版本门禁的地址、结构和控制流；
4. 多关卡、多对象统计得到且能被反例测试推翻的推断；
5. 为可玩性写的现代默认值。

第 5 类必须标记为 `remake_editorial` 或 `unresolved_remake_default`，默认
不能冒充原版。最终有两种处理方式：用证据替换；或放入明确命名的“现代
增强”开关，并让“严格复刻”配置完全不依赖它。

当前产品已经落实这条边界：默认 `original` 不构造编辑型任务导演或 AI
协调器，也不会播放 `remake_editorial` 任务 cue；恢复出的十二张任务简报
与 m011 结局仍正常工作。只有玩家显式选择 `easy/normal/hard`，43 个节奏
节点、45 行补写对白、镜头、教程和增强 AI 才会进入运行时。

## 4. 技术路径

### 4.1 内容编译层

`.NET ResourceFormats/ResourceTool` 负责一次性把稳定 MOD 编译成版本化的
现代中间格式：

- GFL/DBL → 图片、动画、声音、对象定义与引用清单；
- VWF L1—L5 → 地形、视线、移动、事件、手工修正层；
- SLIST1 → scene、阵营、生命、方向、姿态、背包、巡逻和任务锚点；
- SPR1 → 全动作、八方向、逐帧保持、锚点和多层绘制数据；
- 任务控制流取证 → 可审阅的任务图、事件条件和世界副作用；
- 所有输出带源 profile、源索引、源字段、哈希和证据状态。

Godot 不直接猜测旧二进制格式。解析器遇到未知版本、越界、重复索引或
不匹配哈希立即停止。

### 4.2 现代运行时

Godot 运行时分为以下独立系统：

- 世界加载与四绘制队列；
- 静态导航、动态占位、门和交互障碍；
- 角色动作状态机和逐帧命中窗口；
- 选择、命令模式、快捷键和上下文光标；
- 背包、武器、弹药、拾取、丢弃、诱饵与声音；
- 站立/匍匐可见区、L2 遮挡、听觉、警报和调查 AI；
- 任务事件总线、目标图、导演、镜头和媒体；
- HUD、F1、F7、M 小地图、五列背包和菜单；
- 原子存档、设置、回放和状态哈希。

现代实现允许更高效的数据结构、批量绘制、异步资源预热和确定性状态机，
但对玩家可观察的规则保持一致。

### 4.3 MOD 对照闭环

每个尚未确认的功能都执行同一流程：

1. 在隔离副本中用稳定 MOD 进入目标关卡和场景；
2. 只向目标游戏窗口发送输入，不移动、锁定或裁剪系统鼠标；
3. 记录窗口内画面、关卡号、角色/scene 状态、输入和时间点；
4. 截图优先本地 OCR，保留压缩图，不上传无必要的原始大图；
5. 在 Remake 重放同一命令序列；
6. 比较状态哈希、角色位置、动作帧、库存、任务目标、AI 和画面基线；
7. 把通过的证据登记到 parity contract，未通过项保持 `partial`。

需要观测原版内存时，只增加版本门禁、只读探针或目标进程内的专用 hook；
不得依赖固定延时和全局鼠标自动化制造“看似通过”的结果。

对照单位必须是可在数秒到数分钟内稳定重放的单一行为，例如“左键开门并
释放 L3 障碍格”“E 键营救 scene 1427”“取得任务文件后解锁下一目标”或
“四个不同角色同时进入四个区域”。工程自动门禁采用“原版控制流/资源字段、
MOD 短轨迹、Remake 目标视口输入和状态断言”的组合证据。不会再构造
原版整关寻路、战斗和通关机器人；这种长流程既容易被随机战斗放大，也不能
有效定位差异。完整通关只作为人工发布验收，不阻塞局部功能的开发和回归。

## 5. 分阶段实施

### P0：内容身份与回归门禁

状态：已完成第一版。

- MOD 仅保留 m000—m011；
- 裁掉 GFL 尾部三个退役简报，原 0—1393 索引不移动；
- 新增稳定 MOD 的 17 文件内容哈希 profile；
- Remake 可直接导入稳定 MOD，试玩包会拒绝旧来源；
- SDK、选择器、任务目录和 Remake 的 12 个 id/标题/VWF 做交叉校验；
- parity contract 成为机器可读的完成度唯一入口。

### P1：取证与差分回放基础设施

- 已建立仅目标窗口的输入探针和版本化运行轨迹格式；
- 已为 MOD/Remake 输出同一 schema 的基础移动轨迹，并完成第一条严格比较；
- 已建立十二关真实输入自然失败基线：稳定 MOD 只接收进程内
  DirectInput，Remake 只接收目标视口事件；双方均由普通敌方战斗造成必要
  角色死亡，再由各自任务 evaluator 判负，累计 708 项比较零差异；
- 已建立十二关原版 `FLIPMISSION` 输入到胜利状态基线：每关 11 个字母、
  22 个进程内/目标视口事件，累计 720 项比较零差异；该基线只证明作弊
  输入与胜利转换链，不计作非作弊完整玩法通关；
- 继续把 MOD 已有 SDK 地址扩展为已证明身份的只读角色、背包、武器、任务和 AI 快照；
- 为加载、移动、选择、攻击、拾取、姿态、视线、声音、警报、存读档建立
  最小差分用例；
- [已完成] 增加十二关双轮 10 分钟真实窗口自动输入的内存、逐关 P95/P99
  和 >50 ms 尖峰门禁；真人完整通关差分仍是独立未完成项。

退出条件：不是直接注入“任务完成事件”，而是真实输入能在两个运行时生成
可比较轨迹。

### P2：操作、动画和数值完全对齐

- [已完成] 980 个 SPR、2,775 组、11,898 帧的动作/方向/triplet/lookup
  管线；RowLookup 已按原版 32 像素列接入静态对象、移动角色、门两态、
  拾取物、爆炸物和特殊对象，并通过十二关世界像素差分；
- 对齐 F1—F7、M/W/A/S/B/R/C、1—0、Ctrl/↑ 和鼠标全部语义；
- 完成所有上下文光标、命令落点和定时进度条；
- 对齐角色选择后的镜头定位和只在选中时显示的头顶血条；
- 对齐 20 动作、八方向、受伤、死亡、攻击命中帧和多层 anchor；
- 恢复每名角色的初始物品和武器直接数量；原版已确认不存在弹匣/备弹抽象；
- 恢复 type 8/10/11 及地雷、手榴弹、炸药、油桶的精确参数。

退出条件：输入/物品/武器/动画 domain 全部 `verified`。

### P3：地图、路线、感知和 AI 完全对齐

- [已完成] 十二关内容身份、L1—L5、四绘制队列、19,199 个实体及
  1024×768/1920×1080 世界视觉基线；地图/图层渲染 domain 已 verified；
- [已完成] 十二关逐格检查 L2/L3、四绘制队列、墙、门和前景遮挡；
  117,112 个静态 Layer 3 格和 6,710 个编码非角色 scene 足印在真实
  主运行时保持权威；
- [已完成] 恢复非角色静态足印、96 个门的开关和交互后增量导航更新；
- [已完成] 516 条巡逻路线的 656 名敌军四检查点运动学，以及十二关玩家
  往返障碍路线的每 5 帧轨迹形状；4 像素门限下最差 3.420 像素；
- [已完成] 双角色窄通道换位的目标保留、阻挡重规划、确定性优先级和
  静态断路反例；
- 恢复站立/匍匐两段可见扇区、方向更新与障碍裁剪；
- 恢复每类敌人的听觉、攻击距离、反应时间、警报传播和搜索结束条件；
- 恢复尸体发现、诱饵拾取、声音调查、增援、包抄和有限攻击者规则。

退出条件：十二关地图/AI 轨迹可重复，角色不穿墙、不倒跑、不在不可见区
错误发现玩家。

### P4：十二关任务逐关闭环

按 m000 → m011 顺序，每关必须具备：

- 原简报与真实初始状态；
- 所有必要任务世界事件只能由实际玩法产生；
- 一条完整胜利轨迹；
- 时间到、关键人物死亡或关卡特有条件的主要失败轨迹；
- 每个目标前后至少一个存档/读取继续点；
- 对白、镜头、教程、过场和关卡间推进；
- 弹药、任务物品和撤离条件不存在死局；
- 1024×768 和 1920×1080 的地标/HUD/遮挡截图基线。

m006、m008、m009 与 m011 的实际控制流和按简报补写流程同时保留“严格原版”和“修正规则”数据，
现已由“任务规则”设置显式选择并写入会话存档，默认稳定 MOD；两种 profile
分别通过真实世界动作闭环，不能在运行时悄悄混用。

### P5：产品化与最终发布

- 难度、现代增强和严格复刻配置明确分层；
- 十槽存档、快速档、自动档和版本迁移通过十二关中断点测试；
- 窗口、无边框、全屏、多显示器与 IME 屏蔽通过 Windows 10/11；
- 1920×1080/60 FPS：P95 ≤ 20 ms、P99 ≤ 25 ms，无正常游玩同步加载
  >50 ms 尖峰；
- 生成自包含试玩包、清单、版本信息和跨机器验收报告；
- 以 `Test-ModParityContract.ps1 -RequireComplete` 作为发布硬门禁。

## 6. 逐关验收矩阵

所有关卡使用相同的四条必测路径：

| 关卡 | 名称 | 专项验收 |
|---|---|---|
| m000 | 营救行动 | 彭鑫/老罗叔营救、护送、撤离、关键人物死亡 |
| m001 | 奇袭火车站 | 军服、司机、两爆破点、登车与限时 |
| m002 | 劫狱 | 牢区路线、营救/护送、物品闭环和失败条件 |
| m003 | 铁路桥 | 爆破布置、桥目标、撤离及爆炸时序 |
| m004 | 火烧粮仓 | 计划书携带者、火攻/爆破目标和高密度导航 |
| m005 | 大闹寒江镇 | 简报与数据差异定案、城镇门/墙和多目标顺序 |
| m006 | 惩罚 | 稳定规则的双目标+强子取得物品 101；修复规则的接头、追踪和撤离 |
| m007 | 脱困 | 多护送对象、跟随队形、死亡和全员出口 |
| m008 | 暗战矿坑 | 稳定规则四处布置后撤离；修复规则另验手动/提前引爆 |
| m009 | 夺宝奇兵 | 两文件、守军、四列车目标及严格/修正规则 |
| m010 | 血色渡口 | 四个区域分别存在指定四人之一、同帧判定及限时 |
| m011 | 破袭机场 | 稳定规则 scene 1353+老赵/强子到 scene 1359；修复规则指挥官、六目标和撤离 |

每条验收记录都要包含源 profile、构建提交、输入轨迹、状态哈希、压缩窗口
截图、性能摘要和失败原因。缺少其中任一项只能保持 `partial`。

## 7. 当前真实状态

现有工程已经具备完整资源管线、十二关浏览和任务图、导航/动态占位、实际
战斗、背包/投射物、基础 AI、菜单/小地图、存读档和本地试玩包，因此不是
从零开始。十二关产品输入旅程现已增加为硬门禁：415 项断言通过 648 个
仅限目标视口的键盘/鼠标事件，逐关验证快捷选人和镜头、移动、姿态、
W/A/M/F1/Esc、S/B、普通攻击、隔离物理快速存读、主要失败与键盘重玩。
它补足了此前仅第一关具备帧输入回归的问题，但仍不冒充十二关 MOD 对照的
完整真人胜利轨迹。`real_mission_world_loop_test.gd` 还会在真实导入场景中
向目标视口发送 E 键，逐关执行营救、拾取、任务掉落、炸药、爆破和撤离，
并与战斗、占区等产品入口共同闭合任务；当前 750 项断言、152 次世界动作和
166 个目标视口输入事件通过。每关初始状态与每个稳定 MOD 必需目标完成点都会
写入物理磁盘，再逐份重建完整关卡并核对规范化会话哈希。独立真实失败矩阵
进一步以 43 条全新关卡轨迹逐名覆盖解析出的必要队员、全部 8 个护送 scene、
m001/m007/m010 精确限时边界和 m008 修复规则提前引爆，并用非必要角色死亡
与稳定 m008 手动引爆验证不会误判；因此产品侧已声明失败分支已经闭合。
完整真人胜利流程改列为人工发布验收；自动化继续只处理可定位、可重复的
关卡专属交互、演出、数值和画面差异。失败轨迹和作弊胜利转换已有十二关
逐关证据，但后者只验证状态转换，不冒充玩法通关。

十二关结构化地图/实体/层/任务地标基线现已完成并进入门禁；S/B 的目标
过滤、type 90/78、路径/计数和存档边界也已地址级恢复。actor 62 的
效果粒子构造、资源、随机散布和五轮动画寿命也已恢复。原版
`sub_44D980/sub_44E2D0/sub_44E000/sub_44EF50` 的 RowLookup 逐列稳定排序
现已覆盖静态/动态/交互对象；12 个经典视口和 48 个现代分块均通过 RGB565
对照。`sub_455E30` 也定案了 walk/run/crawl triplet 的第 0/2 分量消费和
middle 的十二关边界。导航域的十二关实际路线形状、117,112 个静态格和
窄通道会车门禁也已闭环。原版 SAV/SI 又完成格式解析、三组正式存档转换和
真实关卡恢复，存读档/设置/战役进度 domain 因而闭环。当前 14 个 parity
domain 中 7 个 verified，仍有 19 项明确差异，主要是编辑性对白、完整全局
随机调用顺序、上下文光标像素、HUD 像素、声音触发和稳定 MOD/Remake 两边
的真人完整输入通关轨迹。
它们已全部写入
`mod_parity_contract.json`；函数级世界动作门禁虽然远强于直接发送任务
事件，仍不能代替窗口输入、AI 路线、战斗节奏和演出的全过程对照。

十二关性能门禁也已形成可执行证据：Windows 10 19045、GTX 1050 Ti、
Godot 4.7.1 Compatibility renderer、1920×1080/60 FPS 下，双轮
600 秒采样 33,125 帧和 2,466 个仅目标视口输入事件；整体 P95
18.167 ms、P99 20.936 ms、最大 47.976 ms，所有关卡均有敌军实际移动，
没有 >50 ms 帧，第二轮静态内存增长 1,063,804 字节，24 次载入全部低于
3.4 秒。该结果消除了
`performance_and_windows_release` 中“没有十分钟/十二关 P95/P99”的缺口，
但 Windows 11、更多显卡和跨机器发布包仍保持 `partial`。

2026-07-31 的逐列 RowLookup、稳定巡逻证据路径和 recovered A* 紧凑化
合并后，又执行了完整 `Verify.ps1` 与独立短门禁。全量门禁覆盖 12 关
19,199 个实体、真实任务世界闭环、30 次目标中途物理存档恢复、415 项
产品输入断言和 648 个目标视口事件；最终 48 秒窗口采样为 2,160 帧，
P95 17.703 ms、P99 20.323 ms、最大 34.686 ms，各关均无 >50 ms 帧。
首关第一次移动原先会把 WAV 冷解码与 A* 叠加到同一帧；现在关卡构造期
静默预热当前队员的全部应答变体，实际命令音频耗时从约 7.5 ms 降至
0.2 ms，且不播放、不改变随机/变体顺序。所有运行时可达多格足印也会在
角色注册结束后统一用紧凑字节位图预计算一次，不再由每名角色构造或首次转向
反复扫描整张地图。性能探针继续明确记录 `global_pointer_control=false`。

2026-08-01 又建立了十二关约五秒的全局随机运行态基线：772 名活动角色、
429,872 次干净窗口调用、669 次路线重置、968 次静止重置、11,409 次
追击调用、140 条持久追击关系及 58 组五点搜索均有原版进程内证据。
m002/m005 另以 505,308 条调用定案 80° 环境粒子场。Remake 已接入共用
计数器、持久追击、存档相位和批量粒子事务，并用压缩足印障碍掩码把最密集
m005 的两次独立短门禁稳定到 P99 22.251/21.861 ms。取证方法、逐关数据及
仍不能关闭的边界见
[原版全局随机运行态基线](CRT_RUNTIME_STATE_BASELINE.md)。

## 8. 已落地的 MOD/Remake 差分闭环

2026-07-29 已完成 `m000-basic-movement-v1` 与
`m000-obstacle-route-v1`，它们不是只在 Remake 内部自测：

- `SDK/schemas/runtime-parity-trace-v1.schema.json` 固定跨运行时轨迹格式；
- `Patch/analysis/tools/ModRegressionProbe.cs` 在隔离的稳定 MOD 中采集轨迹；
- `validation/baselines/mod/` 保存两份可审阅的 MOD 基线；
- `game/tests/parity_runtime_probe.gd` 在 Remake 中重放相同的地面命令；
- `tools/Compare-RuntimeParityTrace.ps1` 比较关卡身份、检查点、角色身份、
  位置/目标、移动向量、朝向和时间区间；
- `tools/Run-RuntimeProbe.ps1` 与本地真实资源 `Verify.ps1` 均采用严格比较，
  一旦既有基线产生差异就失败，而不是只输出警告。

探针不移动、锁定或裁剪系统鼠标，不抢占桌面输入；MOD 侧只读取目标进程
快照，并把命令投递到该测试窗口的私有回放队列。首条轨迹确认强子从 MOD
原始位置执行两次无遮挡命令时，与 Remake 在 12 像素移动容差内零差异；
后续 SPR 字段取证已把早期 260 px/s 标量校准替换为原版 60 Hz 分量模型：
secondary 第 0/2 分量分别钳制 X/Y，常见走/匍匐为 120/60 px/s，跑步为
360/180 px/s。到达当前路径点后不把 tick 余量带入下一段，但精确到点会
同 tick 推进游标，不产生额外停顿。

第二条轨迹覆盖第一关开局树丛：原版允许在一个正交侧格开放时贴着单侧
障碍斜行，但两个侧格都阻挡时仍禁止穿角；绕开树木后会尽早回到原移动
走廊，而不会沿等价 A* 路线长期偏移。Remake 现按原八方向等成本图执行
确定性 A*：Chebyshev 启发式、原方向扩展顺序、轴向平局约束和等成本阶梯
规范化共同固定路线与停步朝向。两条玩家轨迹均为零差异，且已进入真实资源
`Verify.ps1` 的严格门禁。

最初两条证据只覆盖第一关；当前门禁已经扩展为 m000—m011 各一名真实
玩家的跨障碍往返，并在观察窗口内每 5 个物理帧采样位置。比较器同时检查
原版采样点到 Remake 规划折线、Remake 实际点到原版轨迹、Remake 实际点
到自己的规划折线，十二关在 4 像素硬门限内全部通过，最差 3.420 像素。
再加上完整静态足印普查和双角色窄通道换位/静态断路回归，导航 domain
现已标为 `verified`；这不等于 AI 战术决策域已经完成。随后建立的
`validation/identities/mod/m000-runtime-actors-v1.json` 已把第一关 62 个
动态阵营对象全部严格映射回 VWF；其中强子是 VWF scene 1436 / DBL 924，
54 名敌军全部进入巡逻审计。早期保守留下的两组任务脚本四人编队，现通过
“已解析前后邻居之间，同 runtime type、runtime/VWF 两侧连续且等长”的
约束恢复，没有用最近位置或名称强行配对。

身份取证同时纠正了旧字段解释：原版活动对象数组下标不是 VWF
`scene_index`，`RuntimeActorV1 +0x064` 也不是 DBL entry，而是对应
`database_header_values[2]` 的运行时类型；真正的 DBL id 仍来自
`database_entry_id`。这条区分已由版本化 schema、生成器和 CI 测试固定，
之后所有战斗、物品和 AI 轨迹只能引用已证明的身份。

`m000-enemy-patrol-v1` 采集了全部 54 名敌军、4 个检查点和两个一秒区间。
原版有效巡逻对角位移为 `sqrt(120²+60²)=134.164 px/s`，活动对象从 VWF
`reference_x/reference_y` 初始化，而 `x/y` 经常是动作/巡逻终点；跨越
端点的样本恢复出约 2.1 秒停留。Remake 已采用相同出生语义、基础速度和
端点等待，并把等待/在途状态纳入存档。该证据链已扩展到 m000—m011，
固化 772 个运行时身份中的全部 656 名巡逻敌军时间线。严格门禁逐一核对
身份、逐角色位移、最大/P90 位移、移动数和静止数；十二关成对 MOD/Remake
复测均为零运动学差异。完整连续逐帧路线相位仍作为诊断输出，不能用有限
检查点冒充任意时刻逐帧等价。

听觉也已从“距离内持续看见玩家”改为事件通道：安静站立的角色不会仅因在
640 像素半径内被全向锁定；按键制造声音、丢弃物、枪声和爆炸才产生调查
或警报。纯声音只传递世界位置并进入 SEARCH，不提供可穿墙持续跟踪的
`current_target`。普通敌方枪声、640 参数警戒、特殊角色覆盖、尸体发现
和原版搜索/返回计时均已进入差分回归。

`m000-natural-contact-v1` 已把第一条真实感知闭环加入严格门禁。稳定 MOD
与 Remake 都让 scene 1436 的强子沿同一组地面命令进入 scene 1598 敌军
的自然视野，scene 1598 最终必须同时满足 `contact_state=1`、
`target_lost=0`，且 `interest_scene_index` 与 `target_scene_index`
都精确指向 1436；触发检查点最多允许相差一个采样点。后续进程私有
`0x45DF71` 取证又证明其第一枪会向 scene 1433 与 scene 1492 写入玩家
坐标，因此 scene 1433 允许随巡逻相位成为第二名 live contact，但不能用
任意其他敌人替代。该场景不是测试代码直接注入目标，因此能阻止“扇区画
对了、实际 AI 却没有看到玩家”这类假通过。取证还纠正了方向值：1—8 是
北、东北、东、东南、南、西南、西、西北，视线角必须先按 640×320
等距椭圆归一化，不能直接对屏幕像素执行 `atan2`。

该轨迹同时把实际战斗纳入严格比较：基线 54 个已解析活动角色在七个检查点
的当前/最大生命与默认攻击类型必须精确相同；强子生命序列固定为
`8,8,8,8,8,6,4`。伤害来源必须属于基线 scene 1598 或后续原生警戒证据
确认的 scene 1433，scene 1598 保持 8 点生命和 type 2 步枪。MOD 运行时
字段也已固定为
`RuntimeActorV1 +0x1C0 = current_hit_points`、
`+0x20C = default_attack_type`，并进入 SDK 静态布局断言。

物品与玩家攻击的首组差分也已闭环。`m001-mine-pickup-inventory-v1`
使用原版 F2 选择 scene 2280，再通过目标窗口的进程私有 DirectInput 左击
scene 2096；MOD 与 Remake 都先寻路到 32×16 邻格，并使武器项目 43
严格 `2→3`。`m000-pistol-attack-inventory-v1` 使用 F4、数字键 5 和真实
敌军 scene 1598，使 scene 1436 的手枪项目 36 严格 `7→6`。新增八条
实机轨迹继续固定步枪 37 `20→19`、机枪 38 `10→9`、飞镖 41
`20→19` 和手榴弹 44 `3→2`；匕首 39、大刀 40 的 mode-1 数量都保持
`1→1`，并严格复现目标 `8→0`；地雷 43、炸药 45 均为 `3→2`，同时
生成一个运行时世界对象。其中第十一关四人出生队形还验证攻击者不会再被
友军动态占位永久堵住。比较器逐检查点核对当前攻击类型和两个有序容器的
index、item、quantity 与 mode；巡逻位置和在途投射物导致的目标生命
截帧只作诊断，不能反向污染严格的容器结论。
`Capture-InventoryParity.ps1` 会从隔离 MOD 重新采集、在 Remake 重放并输出
双份 JSON/Markdown 报告，全程不捕获、裁剪或移动系统鼠标。

第 8 关又补上了此前被阵营筛选漏掉的铁蛋特殊状态。scene 2298 仍占用
原玩家槽 2、可由 F3 选择，但稳定 MOD 的实时 faction 为 1；Remake 现把
“可控角色槽”和“实时敌我阵营”分开建模，并从该 scene 的原始 actor 记录
加载武器/背包双容器。`m007-slingshot-attack-inventory-v1` 先把虚拟光标
移离边缘以消除卷屏竞态，再按住 `Ctrl` 强制攻击相邻古明；两端均得到
项目 42 `1→1`、目标生命 `8→7`。这里也把“进程快照中的 27 名实时
faction 3 玩家”与“玩法中的 28 个初始命令槽角色”明确区分；真实内容
门禁逐关验证 28 名角色、85 个武器项目和 77 个背包项目。铁蛋作为可控
`SquadUnit` 时仍实现原 type 11 注意力保持接口，因此玩家槽、faction 1
和目标 `+656` 三种状态不再互相排斥。

`m010-sight-direct-target-v1` 与 `m010-burial-command-v1` 又闭环了
`S`/`B` 的现场歧义。前者在稳定 MOD 与 Remake 中都通过真实按键和目标点击
选择存活 faction 1 的 scene 1126，并确认没有错误创建 type 90；后者由
大牛使用原匕首制造尸体，再切换到安全位置的老赵执行 `B`，严格得到
`GoalKind=4`，且命令刚接受时不会绕过 `>100` 计数提前生成 type 78。
分离攻击者和掩埋执行者是为了排除第十一关开局敌军随机开火导致的证据竞争，
没有改变玩家可见规则。两条轨迹均可由 `Capture-InventoryParity.ps1`
从已提交基线复抓，比较器只把巡逻位置、经过时间和武器选择瞬态作为诊断。

`m010-briefing-left-click-dismissal-v1` 则确认稳定 MOD 的任务简报位于同一
主窗口、简报期间世界尚未开始，并由进程私有左键点击推进。Remake 现在让
全屏媒体层吞掉左键按下，在松开时只关闭一次简报并恢复世界，避免同一点击
穿透到地图。`Capture-BriefingInputParity.ps1` 会同时重抓稳定 MOD、
向 Remake 目标视口投递事件并严格比较；两端都明确记录
`global_pointer_control=false`。至此 `input_and_hotkeys` parity domain
已由 `partial` 提升为 `verified`。

产品默认难度现为 `original`。它保持生命、伤害、反应、巡逻和感知为 1.0，
关闭重制版瞄准误差、协作增援、同时攻击者调校和所有补写的关内对白、镜头、
教程；该配置甚至不创建编辑导演/AI 协调器。`easy/normal/hard` 仍可在暂停
菜单选择，但明确属于重制增强。选择会持久化并在重开/切关时生效，局内存档
还保存自身难度，读档会先恢复该模式再重建关卡。恢复出的简报与 m011 结局
按来源标签独立保留，不会被上述隔离误伤。

同一轨迹也补回了先前被静态化的五个中立活动对象：scene
1575/1576/1577 三辆卡车保持原位，scene 1621/1622 两只鸡使用原 DBL
身份、阵营、出生朝向、32 px/s 参数和 VWF 巡逻点运行。它们进入动态
占位、投射物目标集合、小地图、运行轨迹以及存读档；任务护送对象也不再
被强制改写原始阵营和朝向。鸡的启动/随机巡逻相位尚未作为逐帧等价结论，
仍保留为后续取证项。

本地复现命令：

```powershell
.\tools\Import-ModAssets.cmd
.\tools\Run-RuntimeProbe.ps1 `
  -GodotExecutable D:\Godot\Godot_v4.7.1-stable_win64_console.exe
```

结果位于 `LocalAssets/qa/runtime-probe/parity/`，包括两份玩家移动轨迹、
一份 54 敌军巡逻轨迹、一份自然接敌轨迹，以及机器可读 JSON 和可读
Markdown/JSON 比较报告。完整十二关巡逻复测使用
`tools/Capture-TwelveLevelPatrolParity.ps1`；拾取/攻击容器复测使用
`tools/Capture-InventoryParity.ps1`；十二关自然交战失败使用
`tools/Capture-TwelveLevelHumanInputNaturalFailureParity.ps1`，CI 的无原版
重放使用 `tools/Verify-TwelveLevelHumanInputNaturalFailureParity.ps1`。

## 9. 每次提交的固定交付

每次完善必须同时完成：

1. 更新源代码和证据状态；
2. 增加或更新自动回归；
3. 运行 `tools/Verify.cmd`；
4. 从稳定 MOD 资源重建 `LocalBuild/1937Remake`；
5. 执行 PCK 与最终 EXE 冒烟；
6. 更新 parity contract 和相关逐关验收记录；
7. 再提交并推送。

这样用户始终运行到本次修改对应的新程序，而不是旧试玩包。
