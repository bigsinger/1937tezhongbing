# 十二关任务恢复说明

## 结论

十二个正式关卡 `m000`—`m011` 的任务骨架不是只能在事后凭感觉重新编排。当前已经恢复并数据化：

- 关卡标题、任务简报图索引和主要目标；
- 目标之间的先后依赖；
- 救援、击毙、拾取、爆破、占点和撤离等条件类型；
- 多目标计数与按 scene 去重；
- 限时、角色损失和特殊失败条件；
- 关卡中的任务标记、爆破检测、出口、出生点和入口坐标；
- 全部必需目标完成后的胜利判定。

这些内容已经整理在 `game/data/missions.json`，并由通用 `MissionState` 读取。当前仍缺少战斗、AI、拾取、救援、爆破和出口等世界系统，所以任务事件还不能在实际游玩中完整触发。对白、镜头、演出先后、触发半径和难度节奏也需要逐关人工校准。

换句话说：任务“结构和胜负骨架”可以数据驱动，不需要从零人工臆造；任务“完整可玩实现和剧情呈现”仍需要工程接线与人工编排。

## 恢复证据链

### 1. VWF/SLIST 关卡事实

十二个正式 VWF 均可解析到精确文件末尾，共有 19,199 条实体记录。每条实体可关联到 DBL 对象名称、类别、世界坐标、参考坐标和可选巡逻数据。

任务相关 DBL 项已确认：

| DBL ID | 原显示名 | 规范化类型 | 用途 |
|---:|---|---|---|
| 1001 | 藏尸处 | `corpse_state` | 尸体/发现状态锚点 |
| 1008 | 检测视线精灵 | `sight_detector` | 视线或剧情检测 |
| 1010 | 进入表示 | `entrance` | 进入点 |
| 1011 | 日军出兵处 | `enemy_spawn` | 敌人出生点 |
| 1018 | 标注 | `marker` | 编辑器中的可见任务标记 |
| 1019 | 检测爆炸精灵 | `explosion_detector` | 爆破目标检测 |
| 1020 | 检测出口精灵 | `exit_detector` | 撤离/占点检测 |

其中 24 个爆破检测和 12 个出口检测都能在 32 像素内唯一配对到一个任务标记。另有 7 个独立任务标记，用于军服箱、剧情 NPC 等叙事位置。位置和配对关系可以机械生成并验证。

### 2. 任务控制流

VWF/SLIST 本身保存场景事实，不保存一份完整的高层任务图。已审计 SAV 也按其结构精确结束，没有附加任务脚本；`M1937.SI0` 中可识别的 IBLOCK 是 320×240 存档缩略图。

目标依赖、计数、限时和胜负分支来自原程序中十二关任务控制流的恢复。简报文本和任务图用于给控制流中的实体指针、计数器和分支赋予可读语义。恢复结果再与 VWF 中实际存在的 scene 和锚点数量交叉验证。

### 3. 新运行时数据模型

规范化任务定义不复制旧引擎代码，而是使用事件条件：

```json
{
  "id": "destroy_airfield",
  "depends_on": [],
  "condition": {
    "event": "trigger_activated",
    "where": {"display_name": "检测爆炸精灵"},
    "required_count": 6,
    "unique_by": "scene_index"
  }
}
```

`MissionState` 对事件名称和 payload 做通用匹配，因此同一个状态机可以处理十二关，而不需要为每关复制一套胜负代码。

## 十二关恢复清单

| 关卡 | 标题 | 实体 | 标记/爆破/出口/出生/入口 | 已恢复的任务骨架 |
|---|---|---:|---|---|
| m000 | 营救行动 | 1,630 | 1/0/1/4/0 | 营救彭鑫与老罗叔 → 护送到撤离点 |
| m001 | 奇袭火车站 | 2,525 | 4/2/1/5/8 | 取得军服、营救铁蛋爹、炸毁两个兵营目标 → 登上火车；限时 9,000 秒 |
| m002 | 劫狱 | 898 | 3/1/1/7/2 | 营救强子、炸毁军需卡车 → 强子与老赵一同撤离 |
| m003 | 铁路桥 | 1,254 | 6/5/1/2/4 | 在铁路桥五处放置炸药 → 全体登车撤离 |
| m004 | 火烧粮仓 | 2,721 | 3/2/0/4/15 | 营救古明、击毙军官并取得计划书、烧毁两座粮仓 |
| m005 | 大闹寒江镇 | 771 | 1/0/0/1/10 | 击毙叛徒阿贵 → 取得其文件 |
| m006 | 惩罚 | 1,470 | 2/0/1/4/7 | 尾随接头 → 刺杀孙大麻子与加藤孝一 → 离开区域 |
| m007 | 脱困 | 2,408 | 3/0/1/5/8 | 营救铁蛋父母与孙小姐 → 全员撤离；限时 1,800 秒 |
| m008 | 暗战矿坑 | 805 | 5/4/1/4/1 | 四处工事入口布置炸药 → 老赵和大牛乘升降机撤离；提前爆炸失败 |
| m009 | 夺宝奇兵 | 1,720 | 4/4/0/3/11 | 炸毁军火列车四个目标；取得交接文件和清剿车站按简报保留为可选目标 |
| m010 | 血色渡口 | 1,629 | 4/0/4/4/11 | 在 3,600 秒内占领四处制高点 |
| m011 | 破袭机场 | 1,368 | 7/6/1/7/8 | 刺杀空军指挥官、炸毁六处机场目标 → 向西北撤离 |
| **合计** | **12 关** | **19,199** | **43/24/12/50/85** | **十二份任务图** |

除表中单独列出的限时或提前爆炸外，十二关数据都包含“必要角色损失”的通用失败条件。具体哪些角色在每一剧情阶段属于必要角色，仍要随着角色生命与护送系统实现而校准。

## 已识别的数据差异

恢复过程保留了不一致信息，避免用一个看似整齐的名称掩盖证据：

- `m005`：简报目标叫“阿贵”，scene 736 的 DBL 显示名却是“阿全”；任务数据使用稳定角色 ID `m005_agui`，并记录这项差异；
- `m006`：剧情中的加藤孝一由旧引擎 type 22 / DBL“王老板”资源承担；任务规则使用剧情角色 ID，而不是把资源显示名当作身份；
- `m009`：原控制流中明确可验证的必要计数是四个列车爆破目标；简报中的交接文件和清剿车站先作为可选目标保留，等待实际运行对照；
- `m011`：原版存在爆破目标指针覆盖缺陷，复刻默认按简报修正，见下一节。

这些项目属于需要人工判读和运行校验的“语义层”，但不会阻止场景坐标、锚点计数和任务状态骨架自动化。

## m011 原版指针覆盖缺陷

第十二关有六个爆破检测实体：scene 1348、1349、1350、1351、1352、1353。任务简报也要求炸毁军机、指挥塔和油库等共六处目标。

原版初始化逻辑却把六个目标指针连续写入同一个存储位置。前五次赋值都被下一次覆盖，最终只剩 scene 1353；随后胜利检查实际上只观察这个最后目标。这是原版实现缺陷，不是关卡中缺少五个锚点。

复刻任务数据的默认意图是：

```text
required_count        = 6
unique_by             = scene_index
intended scene binding = 1348..1353
```

也就是要求六个不同爆破目标，和简报及关卡布置一致。当前通用 `MissionState` 只检查 `display_name` 和 `scene_index` 去重，并不直接对白名单 `scene_bindings` 做校验；由于 m011 关卡中恰好有这六个爆破锚点，后续事件发送器应只从当前关卡已绑定锚点生成事件，并补充 m011 专项测试。未来如果确有保存原版异常行为的需求，可以另加显式的 `strict-original` 兼容选项；当前没有实现这个开关。

## 哪些内容可以自动化

在格式和语义映射已经确认之后，下列工作可以由工具生成或严格校验：

- 十二关地图尺寸、19,199 个实体和全部坐标；
- DBL 资源/显示名/类别关联；
- 巡逻当前航点索引、缓存航点世界坐标与 waypoint；其中持久标志 `+0x0C` 的启停语义尚未得到运行时代码证明；
- 七类任务锚点的 scene、坐标和类型，以及标记/爆破/出口/出生/入口五类锚点的逐关数量清单；
- 36 个爆破/出口检测与标记的一对一近邻配对；
- 十二关任务 JSON 的 schema、目标 ID、依赖、计数、去重字段和限时；
- 运行时目标进度、失败和胜利判定。

这使后续关卡实现以“连接事件”为主，而不是重新猜测每个爆破点和出口在哪里。

## 哪些内容仍需人工校准

静态数据和控制流不能完整给出以下体验层信息：

- 对白文本出现时机、人物语气和字幕停留时间；
- 镜头移动、过场站位、动作衔接和音效同步；
- 检测区域半径、是否要求特定角色、多人同时到达的判定细节；
- 敌人 AI 配合、增援时机、警戒反应和难度数值；
- m005/m006/m009 等名字或简报差异的最终呈现；
- 原版缺陷是修正还是提供兼容模式的产品决策。

因此最合理的流程是：先由恢复数据生成任务骨架和自动测试，再逐关试玩、录制和校准演出，而不是先手工搭十二套脚本再反向猜测原作。

## 当前实现文件

| 文件 | 职责 |
|---|---|
| `game/data/missions.json` | 十二关标题、绑定、目标、依赖、限时和失败条件 |
| `game/scripts/mission_data.gd` | schema 与引用完整性校验 |
| `game/scripts/mission_state.gd` | 通用目标进度、失败和胜利状态 |
| `tools/ResourceTool/OriginalAssetConverter.cs` | 导出 `task_anchors` 并校验逐关锚点清单 |
| `LocalAssets/converted/levels/mNNN/level.json` | 本地生成的实体和任务锚点坐标 |

主场景目前在切关时加载并显示任务目标，但尚未把玩法事件送入任务状态机。下一步应先完成第一关战斗/交互垂直切片，再用同一事件协议连接其余十一关。
