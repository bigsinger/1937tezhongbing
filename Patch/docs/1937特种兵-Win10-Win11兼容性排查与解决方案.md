# 《1937特种兵：敌后武工队》Win10/Win11 兼容性排查与解决方案

> 初次测试日期：2026-07-17
> 文档及分发包更新：2026-07-26，补丁版本 v1.3.2
> 本机验证目录：仓库 `Mod/`（原始来源目录此后只读）
> 适用程序：本目录中的绿色版 `M1937.exe`
> 当前状态：可启动、可进菜单、可进入第一关并正常操作；窗口持续响应，偶发微卡问题已进一步处理。

## 1. 最终方案介绍

这不是一个单一的“兼容模式”问题，而是四条老式 Windows 游戏链路叠加造成的：

1. 原生 DirectDraw 显示路径与现代 Windows 不兼容；
2. 绿色版的资源库检查在当前环境下产生误报；
3. 启动影片和 DirectInput 轮询期间长期不处理 Windows 窗口消息，导致标题栏显示“未响应”；
4. 未同步的 60 FPS 提交与 Windows 桌面合成器相位漂移，造成肉眼可见的偶发重复帧/微卡。

最终方案由两层组成：

- 使用官方 [cnc-ddraw](https://github.com/FunkyFr3sh/cnc-ddraw) 将旧 DirectDraw 调用转换到 Direct3D 9，并负责窗口化、缩放和限速；
- 使用本游戏专用的 `dinput.dll` 兼容层转发系统 DirectInput、补充窗口消息处理，并在内存中跳过误报分支和两个问题启动影片。

磁盘上的原版 `M1937.exe` 没有被修改。最终又为图形层开启 VSync，使测试中的桌面合成丢帧率由 **18.81% 降为 0%**。

最终方案不是单纯打开 Windows“兼容模式”，而是在游戏进程外围补齐旧引擎依赖的运行环境：

| 层次 | 最终处理 | 解决的问题 |
|---|---|---|
| 图形兼容 | cnc-ddraw 7.1.0.0、Direct3D 9、60 Tick、VSync | DirectDraw 报错、严重卡顿、桌面合成丢帧 |
| 输入与消息 | 本游戏专用 32 位 `dinput.dll` 代理，消息泵限频限量 | 菜单、任务简报、关卡循环长期不处理窗口消息，同时不抢占热键 |
| 游戏专用修复 | 校验版本和原字节后进行内存补丁 | 资源库误报和两个问题启动影片 |
| 关卡与设置 | 现代启动中心 + 受校验的原菜单命令路径 | 任意选择 12 关并持久化难度、AI 与显示 |
| 显示模式 | 保留 1024×768 完整原版 UI，再缩放到桌面 | 修复底栏、F1 帮助、M 小地图在宽屏内部画布下丢失 |

所有游戏专用机器码修复都只存在于当前进程内存中。关闭游戏后即消失，原版 EXE、资源、关卡和存档格式均保持不变。补丁也不依赖后台服务或网络连接，可以跟随绿色版目录离线分发。

v1.3.0 进一步恢复了原版关卡入口语义。静态分析确认
`M1937.exe+0x3B60` 的“新游戏”短函数会把
`M1937.exe+0xE7060` 强制写为 1，随后统一加载 1—12 对应的
`1937M000.VWF`—`1937M011.VWF`。选择器用仅属于子进程的
`M1937_START_LEVEL=1..12` 传入关卡；代理验证原立即数字节
`01 00 00 00` 后，在内存地址 `M1937.exe+0x3B66` 写入所选值。
普通启动不设置变量，代码和第一关行为都保持原样。

新版本还挂接原菜单的按钮轮询函数；自动进入所选任务时返回原版
“开始游戏”命令 ID，因此会执行原来的资源释放、任务简报、VWF 加载和
状态初始化，不再依赖鼠标坐标。`rungame.ini` 的 `[mod]` 保存难度、
AI、听觉/警报半径、卷屏、IME、计时、显示模式和上次关卡。

v1.3.2 又修复了自动开始路径中的一个输入残留：菜单命令钩子已经能够
直接返回“开始游戏”，但旧测试辅助代码仍把 `M1937_AUTO_START=1` 当成
合成鼠标许可，任务全局值建立前会持续把指针推向旧按钮坐标。现在只有
显式 `M1937_AUTOTEST=1` 的后台探针能够合成输入，普通启动永远只读取
真实 DirectInput 状态。默认显示模式同时改为无边框最大窗口，边缘卷屏
热区从 8 缩到 2 个逻辑像素，并在失焦或坐标越界时立即清零速度。

## 2. 补丁使用介绍

### 2.1 已安装目录直接使用

游戏目录中新增或替换的兼容文件如下：

| 文件 | 作用 |
|---|---|
| `ddraw.dll` | cnc-ddraw 7.1.0.0，替换原生 DirectDraw 显示路径 |
| `ddraw.ini` | 本游戏验证过的窗口、渲染、限帧和 VSync 参数 |
| `dinput.dll` | 本游戏专用 DirectInput 代理和运行时修复层 |
| `cnc-ddraw config.exe` | 图形兼容层设置程序 |
| `启动现代增强版.cmd` | 推荐双击入口 |
| `选择关卡.ps1` / `关卡名称.json` | 现代启动中心与十二关标题数据 |

正常使用时，双击：

```text
游戏目录\启动现代增强版.cmd
```

快捷键：

- `Alt+Enter`：窗口/全屏切换；
- `Ctrl+Tab`：临时释放被游戏锁定的鼠标；
- `Alt+PageDown`：最大化窗口（cnc-ddraw 默认热键）。

### 2.2 使用分发包安装

分发包文件名：

```text
1937特种兵-Win10-Win11兼容补丁-v1.3.2-20260726.zip
```

压缩包不包含原游戏，只包含补丁运行文件、安装/卸载脚本、使用帮助、完整技术说明、许可证和本项目自研兼容层源码。

安装步骤：

1. 解压压缩包到任意目录；
2. 双击 `Install-Patch.cmd`；
3. 输入 `M1937.exe` 所在游戏目录；
4. 安装器校验主程序 SHA-256，备份目标目录中已有的同名兼容文件；
5. 安装完成后双击游戏目录中的 `启动现代增强版.cmd`。

也可以在命令行指定目录：

```bat
Install-Patch.cmd "<游戏目录>"
```

卸载时双击 `Uninstall-Patch.cmd` 并指定同一游戏目录。首次安装备份保存在游戏目录下的 `.1937-compat-backup-before-v1.1.0`，卸载脚本会恢复其中原有文件，并保留备份本身。v1.1.1 沿用此目录名，以便升级安装继续使用真正的首次安装前备份。安装和卸载还会识别 v1.1.0 曾分发的 15 个 Shader 文件：只有路径和 SHA-256 都与旧发布物完全一致时才会删除；修改过的文件会保留并显示警告。

### 2.3 手动安装

不运行脚本时，可将包内 `payload` 文件夹中的所有内容复制到 `M1937.exe` 所在目录。需要复制的是文件夹内部内容，而不是把整个 `payload` 目录再嵌套一层。

### 2.4 分发包内容

| 包内项目 | 用途 |
|---|---|
| `README-使用帮助.md` | 面向最终用户的安装、启动、全屏、卸载和常见问题说明 |
| `Install-Patch.cmd` / `Uninstall-Patch.cmd` | 版本校验、备份、安装及恢复 |
| `payload\` | 实际需要复制到游戏目录的全部运行文件 |
| `补丁技术说明.md` | 本文档的分发副本 |
| `source\dinput-proxy\` | 自研 DirectInput 兼容层的可审查源码和构建脚本 |
| `LICENSE-cnc-ddraw.txt` | cnc-ddraw MIT 许可证 |
| `SHA256SUMS.txt` | 除清单自身外全部分发文件的完整性校验值 |

### 2.5 分发前验证

当前发布流程不是只检查文件是否存在，而是执行完整安装流程：

- 在仅含目标 `M1937.exe` 的最小目录中成功安装；
- 核对 `ddraw.dll`、`dinput.dll`、本地 DLL 重定向、中文启动文件、使用帮助及技术文档均复制成功；
- 核对最终配置仍为窗口启动、Direct3D 9、60 Tick、VSync、动态桌面分辨率全屏；
- 用预先存在的 `ddraw.ini` 哨兵文件验证首次安装备份，并在卸载后逐字恢复；
- 用不匹配的假主程序验证 SHA-256 拒绝路径，未产生补丁文件或备份；
- 卸载后原 `M1937.exe` 保留，所有运行补丁文件被移除，恢复备份继续保留；
- 从 v1.1.0 直接升级时，15 个未修改的旧 Shader 文件和空目录均被安全清理；哈希不匹配的文件会保留；
- 检查包内不含原版 `M1937.exe`、GFL、VWF、SVT、SAV 或其他游戏资源；
- staging 中不含 Shader 或嵌套压缩包。

正式发布时还要从最终 ZIP 全新解压并重复安装、卸载、错误版本拒绝和 Windows Defender 扫描；最终 ZIP 的完整性以同目录 `.sha256.txt` 为准。

最终交付文件：

```text
../release/1937特种兵-Win10-Win11兼容补丁-v1.3.2-20260726.zip
```

| 属性 | 值 |
|---|---|
| 大小 | 以最终发布 ZIP 文件属性为准 |
| SHA-256 | 以同名 `.sha256.txt` 为准 |
| 包内文件数 | 28（含 `SHA256SUMS.txt`） |
| 原游戏内容 | 不包含 |
| 最终 ZIP 安装/卸载回归 | 通过；原 EXE 保留，补丁文件完整移除 |
| Defender 最终压缩包检测 | 分发前建议在下载端按需扫描 |

同目录提供 `1937特种兵-Win10-Win11兼容补丁-v1.3.2-20260726.zip.sha256.txt`，便于分发后校验下载完整性。

## 3. 故障排查过程

### 3.1 `DirectDraw Error DDERR_UNSUPPORTED`

原始报错位置：

```text
DirectDraw Error DDERR_UNSUPPORTED
FILE: E:\IntuitionEngine\IEngineGraphic\IEngineScreen.cpp
LINE: 521
```

游戏依赖 Windows XP 时代的 DirectDraw 表面、显示模式和锁定行为。在 Win10/Win11 上直接调用时，部分操作会返回 `DDERR_UNSUPPORTED`；即使偶尔成功，显示和限速路径也会严重卡顿。

处理方法：把官方 cnc-ddraw 的 `ddraw.dll` 放到游戏目录，使游戏的 DirectDraw 调用在进程内转换为现代渲染后端。最终选择 `renderer=direct3d9`，原因是这台机器上它能够稳定创建窗口、正确显示 2D 画面，并且 Present 调用耗时很低。

结果：`DDERR_UNSUPPORTED` 消失，窗口画面正常出现。

### 3.2 `Can't Load Game Files Library!`

该弹框并非资源文件真的丢失。排查时确认：

- `1937Resources.GFL` 和 `InterMedia.GFL` 均存在且能成功打开；
- 资源归档读取和解包流程已经通过；
- 弹框来自绿色版自身的旧环境检查/返回值判断，是误报。

没有采用“每次自动点击弹框”的临时方案，而是在启动时仅修改进程内存中的条件跳转：

- RVA：`0x0000734A`；
- 预期原字节：`74 0C`；
- 运行时替换：`EB 0C`。

补丁带有原字节校验；程序版本不匹配时不会盲目写入。

结果：弹框从根因处被抑制，游戏资源仍由原流程加载。

### 3.3 启动动画长时间“未响应”

两个启动影片为：

- `GamekingLogo.svt`；
- `1937Intro.svt`。

老影片播放/等待循环在当前系统上耗时很长，且期间不取出 Windows 消息队列中的消息，因此系统把窗口标为“未响应”。继续等待后可能进入菜单，但等待时间和体验不可接受。

处理方法：在内存中将只负责排入上述两个启动影片的 29 字节调用序列改为 NOP：

- RVA：`0x0000762C`；
- 长度：29 字节；
- 同样先校验完整原字节，再执行内存修改。

此修改只跳过片头，不删除 `.svt` 文件，也不跳过任务/关卡视频。

结果：启动直接进入菜单路径，不再经历片头假死。

### 3.4 点击“开始游戏”及任务简报阶段“未响应”

进一步测试发现，问题不只出现在片头：菜单、任务简报和游戏循环中的某些 DirectInput 轮询也会长时间占住线程，却不分发窗口消息。

专用 `dinput.dll` 的处理方式：

1. 从 Windows 系统目录加载真正的 `dinput.dll`；
2. 转发 `DirectInputCreateA` 及原 DirectInput 设备接口；
3. 在 `Acquire`、`GetDeviceState`、`GetDeviceData` 前处理最多 64 条待处理窗口消息；
4. 保留 `WM_QUIT`，正常分发键盘、鼠标、绘制和窗口状态消息。

这样没有改变游戏输入语义，只补上现代 Windows 判断窗口是否响应所需的消息泵。

结果：启动、菜单、任务简报和第一关地图阶段均持续 `Responding=True`。

### 3.5 可玩但偶尔“顿一下”

#### 排查假设

本机环境有几个潜在因素：

- 游戏位于 `ST1000DM003` SATA 机械硬盘；
- 同时存在 60 Hz、144 Hz 和虚拟显示适配器；
- 当前配置同时限制显示帧率和游戏逻辑 Tick；
- `vsync=false` 时，固定 60 FPS 与 60 Hz 桌面合成器可能发生相位漂移。

为避免截图采样自身影响游戏，使用 Intel 官方 [PresentMon](https://github.com/GameTechDev/PresentMon) 1.10.0 直接采集 Direct3D 9 Present 事件，并由自动测试进入第一关。

#### 原配置结果

原配置为 `maxfps=60`、`maxgameticks=60`、`limiter_type=4`、`vsync=false`。

第一关稳定阶段的 11 秒样本：

| 指标 | 结果 |
|---|---:|
| Present 数量 | 638 |
| 帧间隔 P50 | 16.672 ms |
| 帧间隔 P95 | 17.235 ms |
| 帧间隔 P99 | 17.409 ms |
| 最大帧间隔 | 17.856 ms |
| 超过 25 ms | 0 |
| 被桌面合成器丢弃 | 120（18.81%） |

这说明游戏和显卡有能力稳定地产生约 60 FPS，卡顿不是 GPU 性能不足，也不是这一段中机械硬盘阻塞了渲染；问题在于部分已提交帧没有被显示，玩家会看到间歇性的重复帧。

#### 方案对比

- `vsync=true`：丢弃帧降为 0，保留固定 60 FPS；
- `maxfps=-1`（自动跟随屏幕刷新率）：在本机多显示器/虚拟显示环境中误判，后段退化到约 10 FPS，已否决；
- 切换 OpenGL 或降低 D3D9 滤镜：现有 Present/GPU 开销已经很低，没有必要增加兼容性变量；
- 强制单 CPU 核：cnc-ddraw 自身也提示可能影响性能或声音，当前无冻结且单核未满载，因此保持关闭。

#### 最终长时间回归

早期稳定配置使用固定 60 FPS + VSync。第一关连续 21.52 秒有效样本：

| 指标 | 结果 |
|---|---:|
| Present 数量 | 1292 |
| 帧间隔 P50 | 16.667 ms |
| 帧间隔 P95 | 17.100 ms |
| 帧间隔 P99 | 17.458 ms |
| 最大帧间隔 | 18.977 ms |
| 超过 25/33/50 ms | 0 / 0 / 0 |
| 被桌面合成器丢弃 | 0 |
| “未响应”采样 | 0 |
| 单个逻辑核心占用 | 约 21.1% |
| 关卡阶段进程读取量 | 约 167 KB |

因此，早期发现的 DWM 重复帧已通过 VSync 修正。后续界面回归检查又发现，
代理在每次 DirectInput 轮询时无上限地清空 Windows 消息队列，会与
`PeekMessage` Tick 限速争用，并可能提前取走 F1、M 等键盘消息。v1.3.1
将代理消息泵节流为每 8 ms 至多处理 4 条维护消息，并明确排除全部键鼠消息；
cnc-ddraw 同时改用 `BltFast` Tick 限速路径，避免两套 PeekMessage 循环竞争。

## 4. 最终核心配置

`ddraw.ini` 中对本游戏起关键作用的全局项如下：

```ini
[ddraw]
windowed=true
maintas=true
renderer=direct3d9
maxfps=60
vsync=true
adjmouse=false
noactivateapp=true
maxgameticks=60
limiter_type=2
singlecpu=false
toggle_upscaled=true
no_dinput_hook=true
```

说明：

- `maxfps=60` 固定显示提交频率；本机多显示器环境下自动刷新率模式曾误判，
  因此保留已通过 PresentMon 验证的固定值；
- `maxgameticks=60` 限制旧引擎逻辑循环，避免忙等占满一个核心；
- `limiter_type=2` 在 `BltFast` 路径限制逻辑 Tick，避免与代理消息泵竞争；
- `vsync=true` 消除本机实测到的 DWM 合成丢帧；
- `adjmouse=false`、`no_dinput_hook=true` 避免 cnc-ddraw 与本项目
  `dinput.dll` 同时缩放或钩挂相对鼠标位移；
- `singlecpu=false` 避免旧游戏被不必要地锁到一个核心；
- `toggle_upscaled=true` 使 `Alt+Enter` 全屏时采用当前显示器的桌面分辨率；本机当前为 1920×1080，系统分辨率改变后无需重新写死宽高；
- 窗口宽高和位置会由 cnc-ddraw 根据用户调整自动保存，不属于补丁关键参数。

### 4.1 全屏分辨率验证

全屏分辨率没有写死为 `width=1920`、`height=1080`，而是由 cnc-ddraw 在切换时读取当前显示器模式。这样以后把 Windows 桌面改成其他分辨率，游戏全屏也会自动跟随。

本机实测：

| 阶段 | 客户区尺寸 | 窗口位置 | 结果 |
|---|---:|---:|---|
| 切换前窗口模式 | 1024×768 | (440, 125) | 正常 |
| `Alt+Enter` 全屏 | 1920×1080 | (0, 0) | 与主屏当前分辨率一致 |
| 再次切回窗口 | 1024×768 | (440, 125) | 原尺寸恢复 |

测试期间进程始终保持响应。现代启动中心提供以下显示语义：

- “无边框最大窗口”设置 `windowed=true`、`fullscreen=true`，覆盖桌面但
  保留稳定的窗口输入路径，是 v1.3.2 默认推荐模式；
- “全屏缩放”使用 `maintas=true`，保留原始 4:3 比例；
- “全屏铺满”使用 `maintas=false`，把完整的 1024×768 原版画布缩放铺满
  桌面，不留黑边。

早期实验曾使用桌面尺寸创建引擎内部表面。虽然地图可以继续绘制，但原版
只准备了 640、800、1024 三套 UI 素材；没有对应尺寸的底部工具栏、F1
帮助和 M 小地图表面，最终表现为工具栏消失、帮助黑屏、快捷键像是失效。
v1.3.1 默认设置 `PreserveLegacyUI=1` 并忽略旧配置中的
`ExpandedViewport=1`，只允许通过
`M1937_UNSAFE_EXPANDED_VIEWPORT=1` 为分析实验显式启用。所有修改仍只
发生在经过版本校验的进程内存中。

## 5. 完整验证记录

已执行的功能回归包括：

- 冷启动约 8 秒进入主菜单；
- 无 DirectDraw 错误；
- 无资源库误报弹框；
- 启动、菜单、任务简报和地图阶段窗口持续响应；
- 自动进入第一关“营救行动”；
- 实际在地图中下达移动命令，角色正常行走；
- 早期 6 秒、约 30 次/秒的画面采样中，181 个相邻画面对有 136 对发生有效更新；
- 最终 PresentMon 21.52 秒关卡采样无丢帧、无超过 25 ms 的 Present 间隔；
- Windows Defender 分别扫描最终 `M1937.exe` 和 `dinput.dll`，未发现威胁。

偶发卡顿复测资料保存在：

```text
../analysis/results/
```

其中最终原始帧事件为 `pm-final-extended.csv`，对应自动测试进程数据为 `pm-final-extended-driver.csv` 和 `.txt`。

自动化测试曾生成一个 388 字节的测试存档 `1937m010.sav`。它已从本机游戏目录移出；测试存档和原游戏存档均不纳入本仓库。

## 6. 完整性与安全边界

最终文件 SHA-256：

| 文件 | SHA-256 |
|---|---|
| `M1937.exe` | `F4DD1131DF6C993C01EA011F9439BC725E6DC6491B5FBBA47724D7D5B64DA3F3` |
| `ddraw.dll` | `85E0F7D530DFDA134793A57CB3E76B0287DCC96892EE57162DD68F47283B03A9` |
| `dinput.dll` | 每次源码构建生成，见发行包 `SHA256SUMS.txt` |
| `ddraw.ini` | 随显示配置变化，见发行包 `SHA256SUMS.txt` |

`dinput.dll` 只对满足下列特征的程序应用运行时补丁：

- PE32 主程序基址为 `0x00400000`；
- 映像大小为 `0x00124000`；
- 每个补丁位置的原始机器码与预期字节完全一致。

任一条件不满足时，不写入该位置。补丁不联网、不修改关卡、存档和资源文件。

源码和构建脚本位于：

```text
../src/dinput-proxy
```

分发包中另附相同源码：`source\dinput-proxy`。

## 7. 恢复原版

通过 v1.3.2 分发包安装时，优先运行包内 `Uninstall-Patch.cmd`。卸载器会移除 `payload` 清单中的补丁文件；修改过或未知的文件会保留并警告。随后恢复首次安装前保存到 `.1937-compat-backup-before-v1.1.0` 的同名文件；备份目录本身会继续保留。

本机开发目录还保留了补丁制作前的完整原始目录备份：

```text
本机开发备份目录（不纳入发布仓库）
```

关闭游戏后，将下列文件改名即可停用兼容层：

```text
ddraw.dll
dinput.dll
```

如需完全恢复初始目录，可从上述备份复制原文件。游戏关卡、资源和已有存档没有被兼容补丁改写。

## 8. 后续使用注意事项

- 建议把游戏窗口留在当前 60 Hz 主显示器上。固定 60 FPS 在 144 Hz 屏幕上不能整除刷新周期，拖到另一块高刷屏后可能重新出现轻微节奏不均；
- 机械硬盘会让首次进入大关卡或读取大型资源时产生正常的加载等待。最终关卡稳定阶段并未发现磁盘读取引起的渲染停顿；若只在切换关卡时顿一下，可将整个绿色版复制到 SSD，而不是只移动部分资源；
- 不建议为游戏目录设置杀毒软件永久排除项。若第三方安全软件确实在每次读取旧资源时扫描，可先做一次按需扫描并观察，不应直接降低系统安全策略；
- 不要把 `maxfps` 改成 `-1`。本机有多个刷新率和虚拟显示适配器，实测自动刷新率模式会误判；
- 如果手动修改过 `ddraw.ini` 后问题复现，优先恢复本文第 4 节的十个关键参数。

## 9. 参考项目

- [cnc-ddraw 官方仓库](https://github.com/FunkyFr3sh/cnc-ddraw)
- [cnc-ddraw 官方发布页](https://github.com/FunkyFr3sh/cnc-ddraw/releases)
- [Intel PresentMon 官方仓库](https://github.com/GameTechDev/PresentMon)
- [PresentMon 1.10.0 官方发布页](https://github.com/GameTechDev/PresentMon/releases/tag/v1.10.0)
