# 兼容补丁测试报告

测试日期：2026-07-17 至 2026-07-26

## 功能回归

- 冷启动进入主菜单，无 `DDERR_UNSUPPORTED`；
- 不再出现 `Can't Load Game Files Library!`；
- 启动、菜单、任务简报、第一关地图阶段保持 `Responding=True`；
- 自动进入第一关“营救行动”，实际下达移动命令并观察到角色移动；
- `Alt+Enter` 从 1024×768 窗口切换到当前主屏 1920×1080，再切回原窗口尺寸；
- v1.1.1 最终 ZIP 共 24 个条目，不含 Shader、嵌套压缩包或原游戏资源；ZIP 本体及全新解压目录均通过 Microsoft Defender 扫描（安全情报版本 1.455.200.0）。
- v1.2.0 选择器脚本通过 PowerShell 5.1 无 BOM/中文路径解析回归；
- 隔离窗口探针对第 12 关验证 `M1937.exe+0x403B66 = 0C 00 00 00`，
  原版主菜单持续 `Responding=True`；不传选择变量时仍为
  `01 00 00 00`。
- v1.3.1 隔离探针确认旧配置即使残留 `ExpandedViewport=1`，最终游戏与
  渲染画布仍为 1024×768，避免底栏、F1 帮助和 M 小地图资源为空；
- v1.3.1 的 45 秒后台过程采样为 0 次未响应，关卡值、卷屏钩子、敌军
  听觉和警报参数均保持有效；
- v1.3.1 最终 ZIP 共 28 个条目，不含原游戏 EXE、GFL、VWF、SVT 或 SAV；
  在全新目录安装、卸载均通过，卸载后原 EXE 保留且补丁文件无残留。

## PresentMon 帧时间结果

使用 Intel PresentMon 采集 Direct3D 9 Present 事件，比较 VSync 前后状态。

| 配置 | 样本 | P50 | P99 | 最大间隔 | 丢弃 Present |
|---|---:|---:|---:|---:|---:|
| 固定 60 FPS、VSync 关闭 | 638 帧 | 16.672 ms | 17.409 ms | 17.856 ms | 120（18.81%） |
| 固定 60 FPS、VSync 开启 | 1292 帧 | 16.667 ms | 17.458 ms | 18.977 ms | 0 |

最终配置：

```ini
windowed=true
renderer=direct3d9
maxfps=60
vsync=true
maxgameticks=60
limiter_type=2
toggle_upscaled=true
```

“自动跟随刷新率”（`maxfps=-1`）在测试机同时存在 60 Hz、144 Hz 和虚拟显示适配器时会误判，后段退化到约 10 FPS，因此没有采用。

v1.3.1 继续使用已经过 PresentMon 验证的固定 60 FPS + VSync，但把
Tick 限速点从 `PeekMessage` 改为 `BltFast`。代理消息泵从“每次输入轮询
清空队列”改为“每 8 ms 最多处理 4 条维护消息”，键盘和鼠标消息留给
原版主循环，从而减少偶发微卡和热键竞争。

## 安装器回归

- 在只含匹配版 `M1937.exe` 的最小目录中，使用 v1.3.1 staging 安装成功；
- 预置的 `ddraw.ini` 哨兵文件被备份，并在卸载后逐字恢复；
- 使用错误版本主程序时 SHA-256 校验失败，不创建补丁文件或备份；
- 从 v1.1.0 直接升级时，路径和 SHA-256 都匹配旧发布物的 15 个 Shader 文件被清理；修改过的文件保留并警告；
- 卸载后原 `M1937.exe` 保留，补丁 DLL、启动脚本和帮助文件移除。

v1.3.1 最终 ZIP 已在全新目录重复安装/卸载回归：`dinput.dll`、`ddraw.dll`
和现代启动文件均安装成功，旧的 `选择关卡.cmd` 与
`M1937.exe.local` 不会重新出现；卸载后补丁文件无残留，
`M1937.exe` SHA-256 保持不变。外部 ZIP SHA-256 为：

```text
以 release 目录同名 .sha256.txt 为准（文档更新后重新生成）
```

历史 v1.1.1 ZIP 与全新解压目录曾使用 Microsoft Defender 安全情报
1.455.200.0 扫描，未发现威胁；下载 v1.3.1 后仍建议在本机按需扫描。

## 原始数据

关键原始 PresentMon 数据位于 [`analysis/results/`](../analysis/results/)，其中 `pm-final-extended.csv` 对应最终长时间回归。
