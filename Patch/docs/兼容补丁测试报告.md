# 兼容补丁测试报告

测试日期：2026-07-17 至 2026-07-26

## 功能回归

- 冷启动进入主菜单，无 `DDERR_UNSUPPORTED`；
- 不再出现 `Can't Load Game Files Library!`；
- 启动、菜单、任务简报、第一关地图阶段保持 `Responding=True`；
- 自动进入第一关“营救行动”，实际下达移动命令并观察到角色移动；
- `Alt+Enter` 从 1024×768 窗口切换到当前主屏 1920×1080，再切回原窗口尺寸；
- v1.1.1 最终 ZIP 共 24 个条目，不含 Shader、嵌套压缩包或原游戏资源；ZIP 本体及全新解压目录均通过 Microsoft Defender 扫描（安全情报版本 1.455.200.0）。
- v1.2.0 选择器脚本通过 PowerShell 5.1 无 BOM/中文路径解析回归；
- 隔离窗口探针对第 12 关验证 `M1937.exe+0x403B66 = 0C 00 00 00`，
  原版主菜单持续 `Responding=True`；不传选择变量时仍为
  `01 00 00 00`。
- v1.3.1 隔离探针确认旧配置即使残留 `ExpandedViewport=1`，最终游戏与
  渲染画布仍为 1024×768，避免底栏、F1 帮助和 M 小地图资源为空；
- v1.3.1 的 45 秒后台过程采样为 0 次未响应，关卡值、卷屏钩子、敌军
  听觉和警报参数均保持有效；
- v1.3.1 最终 ZIP 共 28 个条目，不含原游戏 EXE、GFL、VWF、SVT 或 SAV；
  在全新目录安装、卸载均通过，卸载后原 EXE 保留且补丁文件无残留。
- v1.3.2 使用独立游戏副本和 `M1937_AUTO_START=1` 回归 35 秒，未发送
  物理或合成鼠标输入即进入第一关；最终逻辑/渲染画布均为 1024×768，
  `Responding=True`，确认自动进关不再推动玩家指针；
- v1.3.2 默认采用无边框最大窗口、`adjmouse=false` 与
  `no_dinput_hook=true`，避免 cnc-ddraw 和本项目 DirectInput 代理重复
  缩放或钩挂相对鼠标增量；边缘卷屏热区缩至 2 像素，窗口失焦或指针
  离开游戏画布时会立即清零卷屏速度。
- v1.3.3 的真实输入回归发现上述 `no_dinput_hook=true` 会切断拉伸窗口
  所需的坐标映射，因此撤销这组默认值。最终默认配置为原生 1024×768
  窗口、`adjmouse=false`、`devmode=true`、`savesettings=0` 和
  `no_dinput_hook=false`；窗口取得前台后，系统鼠标左键按下被游戏内部
  DirectInput 状态读取为 `1`，进程全程 `Responding=True`。
- 故意写入 v1.3.2 的问题无边框参数后，`启动游戏-窗口模式.cmd` 的
  `-SafeWindow` 恢复路径在创建游戏进程前自动还原上述安全值；任务启动
  10 秒后进程仍为 `Responding=True`。
- v1.3.4 删除产品 DLL 中全部鼠标状态合成和自制卷屏代码；源码审计确认
  不再引用 `SetCursorPos`、`mouse_event`、`ClipCursor`、
  `ApplyAutomatedMouseState` 或 `SmoothEdgeScroll`。x86 Release 重新
  编译成功，`Mod/dinput.dll` 与构建产物 SHA-256 完全一致。
- v1.3.4 选择器的所有正常入口（包括兼容保留的 `-StartImmediately`）
  均传入 `M1937_AUTO_START=0`；所选关卡只修改“新游戏”入口的内存立即
  数，玩家随后从原版菜单点击“开始游戏”，避免任务简报被提前推进。
- 实机反馈确认 v1.3.4 的 `devmode=false` 会把系统光标约束在窗口右下角。
  v1.3.5 恢复 `devmode=true`、`adjmouse=false`；通过 Win32 INI 读取
  验证最终值为固定 1024×768 窗口、不缩放且不约束光标。产品源码中的
  鼠标状态改写调用仍为 0。

## PresentMon 帧时间结果

使用 Intel PresentMon 采集 Direct3D 9 Present 事件，比较 VSync 前后状态。

| 配置 | 样本 | P50 | P99 | 最大间隔 | 丢弃 Present |
|---|---:|---:|---:|---:|---:|
| 固定 60 FPS、VSync 关闭 | 638 帧 | 16.672 ms | 17.409 ms | 17.856 ms | 120（18.81%） |
| 固定 60 FPS、VSync 开启 | 1292 帧 | 16.667 ms | 17.458 ms | 18.977 ms | 0 |

最终配置：

```ini
windowed=true
renderer=direct3d9
maxfps=60
vsync=true
maxgameticks=60
limiter_type=2
toggle_upscaled=true
```

“自动跟随刷新率”（`maxfps=-1`）在测试机同时存在 60 Hz、144 Hz 和虚拟显示适配器时会误判，后段退化到约 10 FPS，因此没有采用。

v1.3.3 继续使用已经过 PresentMon 验证的固定 60 FPS + VSync，但把
Tick 限速点从 `PeekMessage` 改为 `BltFast`。代理消息泵从“每次输入轮询
清空队列”改为“每 8 ms 最多处理 4 条维护消息”，键盘和鼠标消息留给
原版主循环，从而减少偶发微卡和热键竞争。

## 安装器回归

- 在只含匹配版 `M1937.exe` 的最小目录中，使用 v1.3.3 staging 安装成功；
- 预置的 `ddraw.ini` 哨兵文件被备份，并在卸载后逐字恢复；
- 使用错误版本主程序时 SHA-256 校验失败，不创建补丁文件或备份；
- 从 v1.1.0 直接升级时，路径和 SHA-256 都匹配旧发布物的 15 个 Shader 文件被清理；修改过的文件保留并警告；
- 卸载后原 `M1937.exe` 保留，补丁 DLL、启动脚本和帮助文件移除。

v1.3.3 最终 ZIP 已在全新目录重复安装/卸载回归：`dinput.dll`、`ddraw.dll`
和现代启动文件均安装成功，旧的 `选择关卡.cmd` 与
`M1937.exe.local` 不会重新出现；卸载后补丁文件无残留，
`M1937.exe` SHA-256 保持不变。外部 ZIP SHA-256 为：

v1.3.4 最终 ZIP 又在只含目标 `M1937.exe` 的全新目录完成安装/卸载：
必需文件全部安装，安装后的 `dinput.dll` 与 `Mod/dinput.dll` 哈希一致；
卸载后补丁文件残留为 0，原 EXE 保留。包内 `SHA256SUMS.txt` 全部条目
复算通过；编译后 DLL 的导入表也确认不含 `SetCursorPos`、`mouse_event`、
`ClipCursor` 或 `SetCapture`。

```text
以 release 目录同名 .sha256.txt 为准（文档更新后重新生成）
```

历史 v1.1.1 ZIP 与全新解压目录曾使用 Microsoft Defender 安全情报
1.455.200.0 扫描，未发现威胁；下载 v1.3.3 后仍建议在本机按需扫描。

## 原始数据

关键原始数据位于 [`analysis/results/`](../analysis/results/)，其中
`pm-final-extended.csv` 对应最终长时间帧回归，
`v132-mouse-autostart-probe.txt` 对应无合成鼠标自动进关回归；
`v133-native-window-mouse-probe.txt` 对应原生窗口真实鼠标输入回归；
`v133-safe-launcher-recovery.txt` 对应错误配置一键恢复回归。
